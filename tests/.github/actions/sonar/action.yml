name: sonar
description: Dispatch sonar to the active module (module_dir).
inputs:
  module_dir:
    required: true
runs:
  using: composite
  steps:
    - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/sonar
      with:
        module_dir: ${{ inputs.module_dir }}
