# Inbox: structural blocks из consumer allurerc.mjs / allure/*, которых ещё нет в _ethalon/allure/.
# Формат блока — см. skill sync-allurerc-ethalon (правило 5).
#
# Пример:
# ## /absolute/path/to/consumer/tests/allurerc.mjs
# // path: plugins.dashboard.options.layout[] (epicCharts extension)
# {
#   "type": "newChartType",
#   "title": "Новый chart"
# }
