import { createAllureConfig } from "./allure/create-config.mjs";

/** Ethalon runnable placeholder (multistack profile). */
export default createAllureConfig({
  slug: "autotests-ai-multistack-app",
});
