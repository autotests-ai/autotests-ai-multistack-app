# Inbox: переименования / переносы structural paths из consumer allurerc.mjs.
# Формат блока — см. skill sync-allurerc-ethalon (правило 6).
# После переноса в _ethalon/allure — удалить блок из этого файла.
#
# Пример:
# ## /absolute/path/to/consumer/tests/allurerc.mjs
# //- plugins.awesome.options.oldKey
# "newKey": "same value"
