// Retired. SSOT is the repository-root Jenkinsfile (GHA tests-lane:
// mock ui + screenshots, api/e2e on stage and prod). Do not point a job here.

pipeline {
  agent none
  stages {
    stage('retired') {
      steps {
        error 'Retired. Point the job at the repository-root Jenkinsfile.'
      }
    }
  }
}
