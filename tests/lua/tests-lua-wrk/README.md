# tests-lua-wrk

`status: slot` · load · official [wrk](https://github.com/wg/wrk) with **Lua** scripts (`layers: [performance]`).

This is the Lua school, not Vegeta (Go binary + HTTP targets) and not a C DSL. Fill in a later window. Student emit: `lua-wrk`.
