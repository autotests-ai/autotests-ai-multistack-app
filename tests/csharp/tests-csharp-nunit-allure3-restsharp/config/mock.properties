# mock — compose mock profile (stand-gateway :9911).
# Prerequisite: docker compose --profile mock up -d stand-gateway

baseUrl=http://localhost:9911
apiBaseUrl=http://localhost:9911/
welcomeUsername=mock-user
