# tests-python-pytest-selenium

`status: slot` · **UI-only** · Selenium. No HTTP client (`layers: [ui, e2e]`).

For students who are not on REST yet. Combo living (UI+HTTP, in-cell **requests**) stays [`tests-python-pytest-requests-selenium`](../tests-python-pytest-requests-selenium/). HTTP-only sibling: [`tests-python-pytest-requests`](../tests-python-pytest-requests/).

Not a generate sibling. Student emit: `python-pytest-selenium`.
