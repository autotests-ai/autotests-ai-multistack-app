# tests-python-pytest-playwright

`status: slot` · **UI-only** · Playwright. No HTTP client (`layers: [ui, e2e]`).

For students who are not on REST yet. Combo living (UI+HTTP, in-cell **APIRequest**) stays [`tests-python-pytest-api_request-playwright`](../tests-python-pytest-api_request-playwright/). HTTP-only sibling: [`tests-python-pytest-requests`](../tests-python-pytest-requests/). There is no HTTP-only `api_request` school.

Not a generate sibling. Student emit: `python-pytest-playwright`.
