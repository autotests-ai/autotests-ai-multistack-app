# Single source for the pinned Chrome for Testing build (browser + matching chromedriver).
# Screenshot folder is chrome-{major}; CI uses the same pin as CHROME_FOR_TESTING_VERSION.
# Per-run override: CHROME_FOR_TESTING_VERSION.
version=148.0.7778.178
