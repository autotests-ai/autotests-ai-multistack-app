name: api
description: STOP — no api layer in the Python tests family.
inputs:
  module_dir:
    required: true
  stand:
    required: true
runs:
  using: composite
  steps:
    - shell: bash
      env:
        MODULE_DIR: ${{ inputs.module_dir }}
      run: |
        echo "STOP: no api layer in tests/python (${MODULE_DIR:-unset})" >&2
        exit 1
