# tests-python-pytest-selene

`status: slot` · **UI-only** · Selene. No HTTP client (`layers: [ui, e2e]`).

For students who are not on REST yet. Combo living (UI+HTTP, in-cell **requests**) stays [`tests-python-pytest-requests-selene`](../tests-python-pytest-requests-selene/). HTTP-only sibling: [`tests-python-pytest-requests`](../tests-python-pytest-requests/). httpx stays HTTP-only in [`tests-python-pytest-httpx`](../tests-python-pytest-httpx/) — not a UI in-cell client.

Not a generate sibling. Student emit: `python-pytest-selene`.
