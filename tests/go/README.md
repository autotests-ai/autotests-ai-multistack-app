# Go tests

| Folder | Role |
|--------|------|
| `tests-go-testing-allure3-net_http/` | **active** — HTTP block: `go test` + net/http + testify + Allure Go; `./cover-config.sh` + `sonar-tests` |
| `tests-go-testing-allure3-playwright/` | **active** — UI+HTTP: playwright-go + in-cell net/http; `./cover-config.sh` + `sonar-tests` |
| `tests-go-cdp/` | mill — IR crystals + `exec greedy run` (crystal column on `/stack/`) |
