# Go tests

| Folder | Role |
|--------|------|
| `tests-go-cdp/` | mill — IR crystals + `exec greedy run` (crystal column on `/stack/`) |
| `tests-go-testing-allure3/` | slot — `go test` + Allure, not CDP |
