# ci — compose gateway on this machine.
# ./gradlew test -Denv=ci -DincludeTags=infra
# ./gradlew test -Denv=ci -DincludeTags=api
# ./gradlew test -Denv=ci -DincludeTags=e2e
# UI+HTTP — Playwright Chromium + APIRequest. HTTP-only Rest Assured is a sibling.
#
# Prerequisite: docker compose up -d (stand-gateway-ci :9821).

# Target app — ci gateway (SPA at /, real backend at /api)
baseUrl=http://localhost:9821

# REST API origin (WireMock admin on mock stand only)
apiBaseUrl=http://localhost:8800/

# Browser configuration
browserSize=1920x1280
headless=true
