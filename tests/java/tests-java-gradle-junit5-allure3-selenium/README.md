# tests-java-gradle-junit5-allure3-selenium

`status: slot` · `layers: [e2e]` · **UI block** · Selenium 4 (Gradle · JUnit 5 · Allure 3).

HTTP client is a separate block (`tests-java-gradle-junit5-allure3-restassured`). Not a generate sibling. Student emit: `java-gradle-junit5-selenium`.
