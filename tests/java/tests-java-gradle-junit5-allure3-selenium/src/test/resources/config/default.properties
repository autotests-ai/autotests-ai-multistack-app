# Runtime defaults — Owner merge, lowest priority. Env profiles override baseUrl / apiBaseUrl / remoteUrl.
# Per-run: -D<key>=<value>

allureReportMode=allure3
attachBrowserConsoleLogs=false
attachLastScreenshot=false
attachPageSource=false
attachVideo=false

baseUrl=
apiBaseUrl=
apiHealthService=backend-java-spring
welcomeUsername=user1

browser=chrome
browserVersion=148
browserSize=1920x1280
headless=false
closeBrowserAfterEach=true
closeBrowserAfterAll=true
skipBlankOpen=false

enableVnc=false
enableVideo=false
remoteUrl=
videoFolder=

logToConsole=true
rootLogLevel=info
