# Runtime defaults. URLs live in config/${env}.properties.
# Default env for this cell is prod (GitHub APK / gradle.properties), not ci.
# No mock, no screenshot flags, no web baseUrl.

apiBase=
backendId=backend-java-spring
androidApiBase=
iosApiBase=
