# ci — docker compose backend-java-spring on this machine (:8800).
# Not mock. Not the /stack/ board. Not the GitHub APK (that APK is prod).
# Bake:  cd mobile/kotlin/mobile-kotlin-compose && ./gradlew :app:assembleDebug -Penv=ci
#        or from this cell: ./gradlew assembleApp emulator -Denv=ci
# iOS:   Appium processArguments from -Denv=ci, or MULTISTACK_ENV=ci scripts/build-sim.sh

# AuthSetup on the laptop
apiBase=http://localhost:8800/api
# APK on the emulator (10.0.2.2 is the host)
androidApiBase=http://10.0.2.2:8800/api
# Simulator / processArguments
iosApiBase=http://127.0.0.1:8800/api
backendId=backend-java-spring
