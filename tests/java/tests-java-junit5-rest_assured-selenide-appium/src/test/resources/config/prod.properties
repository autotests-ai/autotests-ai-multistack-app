# prod — live pair. Same URL as mobile-kotlin-compose/gradle.properties
# and the GitHub Release APK. Default for this cell (not ci).
# ./gradlew selenoid -Denv=prod
# ./gradlew emulator -Denv=prod

apiBase=https://autotests.ai/stack/backend-java-spring/api
backendId=backend-java-spring
