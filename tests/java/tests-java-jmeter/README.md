# tests-java-jmeter

`status: slot` · `layers: [performance]` · Apache JMeter **JMX** (Java tool).

Groovy JSR223 scripts live in `tests-groovy-jmeter`. Not a generate sibling. Student emit: `java-jmeter`.
