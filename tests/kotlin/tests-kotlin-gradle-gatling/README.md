# tests-kotlin-gradle-gatling

`status: slot` · `layers: [performance]` · Gatling **Kotlin DSL** (Gradle).

Java / Scala DSLs are sibling slots. Not a generate sibling. Student emit: `kotlin-gradle-gatling`.
