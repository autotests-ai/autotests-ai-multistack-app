# tests-kotlin-junit5-selenium

`status: slot` · **UI-only** · JUnit 5 · Selenium 4. No HTTP client (`layers: [ui, e2e]`).

For students who are not on REST yet. Combo living (UI+HTTP) stays [`tests-kotlin-junit5-ktor-selenium`](../tests-kotlin-junit5-ktor-selenium/). HTTP-only sibling: [`tests-kotlin-junit5-ktor`](../tests-kotlin-junit5-ktor/).

Not a generate sibling. Student emit: `kotlin-junit5-selenium`.
