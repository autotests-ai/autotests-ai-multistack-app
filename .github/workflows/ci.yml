name: CI

# Teaching tests: when TESTS_LANG=java, two knobs (-Denv / -DincludeTags) pick stand and slice.
# Other TESTS_LANG values run the full active-module suite (no Gradle tag filters yet).
# Every job below is the same command a student types locally.
#
# Active stack defaults live in `env:` once — APP_URL / UI_URL / BASE_URL, GHCR package
# names and RUN_LABEL are derived from them. Swap TESTS / TESTS_DIR / TESTS_LANG (or
# BACKEND / FRONTEND) when the teaching default changes; job ids stay layer names.
#
# Allure: trigger opens one shared TestOps job-run (soft); test jobs stream live and
# publish raw results. publish-allure-report is gating (lock pins, npm ci, generate).
# Pages: that job pushes gh-pages (soft); live deploy is deploy-github-pages
# (upload-pages-artifact@v5 + deploy-pages@v5). Retry without the pyramid:
# workflow_dispatch .github/workflows/pages.yml. TestOps / Telegram stay soft too.
# TestOps UI rerun: workflow_dispatch + ALLURE_JOB_RUN_ID → ALLURE_KEEP_TESTPLAN=true.
# Launch env axes: BROWSER / OS / ENDPOINT / VERSION(=sha) / BRANCH.
# SSOT host: allure.qa.guru (legacy allure.autotests.cloud redirects).
# Pyramid layers (classical): unit · integration (no UI) · api · e2e · manual
# Sideways FE: component (jsdom) — only the active FRONTEND (same teaching knob as deploy).
# Screenshot (@Tag screenshot): compare in ui-mock-tests (every PR) and e2e-tests-stage (stage CD).
# Prod e2e-tests screenshot is dispatch run_screenshot.
# rewrite: update_mock_screenshots / update_stage_screenshots / update_e2e_screenshots, not a CD job.
# CD: push develop → stage only (WIP). Push main → same SHA to stage, then prod
# (prod jobs `needs` e2e-tests-stage). `trigger` maps push paths / dispatch `deploy`
# onto the same flags. Sonar gates deploy, not build.
# Prod deploy moves the **prod** checkout dir to the new SHA.
# Stage twin: deploy-backend-stage / deploy-frontend-stage — own APP_DIR,
# compose project autotests-ai-multistack-app-stage, ports +10000 (deploy/stage.env).
# Stage CD: push develop | push main | dispatch stage|both|production-from-main.
# Skip until vars.STAGE_APP_DIR is set. Live tests: stage = full api/e2e;
# prod = @Tag("prod") after stage e2e is green.
# Stage mutex: main preempts develop (cancel), develop yields if main is queued
# or running. Not one shared cancel-in-progress group — that would let develop
# cancel a prod promotion.
# Roots: trigger → unit-tests · component-tests · tests-harness
#   integration-tests ← unit-tests (backend Spring+PG, pre-build; PR + main)
#   sonar-backend ← unit-tests + integration-tests
#   sonar-tests ← tests-harness (skipped on backend-only lane)
#   ui-mock-tests ← component-tests (jsdom first; mock compose on the runner)
#   sonar-frontend ← component-tests + ui-mock-tests (mock skipped ⇒ sonar still runs)
#   build-backend ← unit-tests + integration-tests · build-frontend ← ui-mock-tests
#   deploy-backend-stage / deploy-frontend-stage ← same gates; cd_stage; skip until vars.STAGE_APP_DIR
#   api-tests-stage / e2e-tests-stage ← deploy-*-stage; full api / e2e vs STAGE_APP_URL
#     (e2e-tests-stage also compares screenshots/stage/)
#   deploy-backend ← build-backend + sonar-backend + e2e-tests-stage (cd_production)
#   deploy-frontend ← build-frontend + sonar-frontend + e2e-tests-stage
#   api-tests ← deploy-backend; -Denv=prod -DincludeTags=api&prod
#   e2e-tests ← api-tests + deploy-frontend; includeTags=e2e&prod excludeTags=mock,screenshot
#   publish-allure-report ← pyramid layers + sonar-backend (gating generate; soft Telegram + Pages)
# push main|develop: trigger infers lanes from paths (tests/ ⇒ no app deploy).
# dispatch `deploy=none|backend|frontend|tests|all` + `deploy_target=production|stage|both`.
# PR (main|develop): no deploy; ui-mock-tests after component-tests (mock compose profile).

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      deploy:
        description: "Lanes: none (tests only, no CD) · backend · frontend · tests (api/e2e vs live stand, no CD) · all. Push infers from paths."
        type: choice
        options: [none, backend, frontend, tests, all]
        default: none
      deploy_target:
        description: "CD host: production (main: stage then prod) · stage (develop or main) · both (same chain as production from main). Push develop→stage only; push main→stage then prod."
        type: choice
        options: [production, stage, both]
        default: production
      run_integration:
        description: "Run backend Spring+PG integration tests (pre-deploy)"
        type: boolean
        default: false
      run_api:
        description: "Run API tests (prod: api&prod; stage/develop: full api layer)"
        type: boolean
        default: false
      run_mock:
        description: "Run ui-mock tests on the runner (flows + screenshot compare)"
        type: boolean
        default: false
      run_e2e:
        description: "Run E2E tests (prod: e2e&prod; stage/develop: full e2e). Stage screenshot compare is always on."
        type: boolean
        default: false
      run_screenshot:
        description: "e2e-tests (prod): compare screenshot PNGs (Selenoid)"
        type: boolean
        default: false
      run_manual:
        description: "Run tests tagged manual against prod"
        type: boolean
        default: false
      include_tags:
        description: "e2e-tests (java): override -DincludeTags. Empty = run_e2e/run_screenshot selection."
        type: string
        default: ""
      exclude_tags:
        description: "e2e-tests (java): add -DexcludeTags independently of include_tags"
        type: string
        default: ""
      update_mock_screenshots:
        description: "ui-mock-tests: rewrite mock screenshot PNGs (skips compare)"
        type: boolean
        default: false
      update_e2e_screenshots:
        description: "e2e-tests: rewrite prod screenshot PNGs (skips compare)"
        type: boolean
        default: false
      update_stage_screenshots:
        description: "e2e-tests-stage: rewrite stage screenshot PNGs (skips compare)"
        type: boolean
        default: false
      ALLURE_JOB_RUN_ID:
        description: "Allure TestOps service parameter. Leave blank."
        type: string
        required: false
        default: ""
      ALLURE_USERNAME:
        description: "Allure TestOps service parameter. Leave blank."
        type: string
        required: false
        default: ""

concurrency:
  # PRs: per-ref, cancel stale. main vs develop cannot share cancel-in-progress
  # (a new develop would abort a prod promotion). Separate groups; jobs
  # preempt-develop-cd / yield-to-main-cd enforce main priority on the stage host.
  group: ${{ github.event_name == 'pull_request' && format('ci-pr-{0}', github.ref) || github.ref == 'refs/heads/main' && 'ci-cd-main' || github.ref == 'refs/heads/develop' && 'ci-cd-develop' || format('ci-{0}', github.ref) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || github.ref == 'refs/heads/develop' }}

permissions:
  actions: read
  contents: read

env:
  IMAGE_TAG: ${{ github.sha }}
  # ── Active stack defaults (change here only) ─────────────────────────────
  BACKEND: backend-java-spring
  BACKEND_DIR: backend/java/backend-java-spring
  # java|kotlin|python|go|javascript|typescript — unit-tests setup/run follows this
  BACKEND_LANG: java
  BACKEND_PORT: "8800"
  # Teaching module: component-tests + sonar + the only SHA image this workflow
  # builds and deploys. Sibling compose frontends stay in docker-compose.yml for
  # the catalog; they are not a CI matrix (students see one FRONTEND knob).
  FRONTEND: frontend-typescript-react
  FRONTEND_DIR: frontend/typescript/frontend-typescript-react
  # Teaching tests module — selenide today; flip TESTS/TESTS_DIR/TESTS_LANG together
  TESTS: tests-java-gradle-junit5-allure3-selenide
  TESTS_DIR: tests/java/tests-java-gradle-junit5-allure3-selenide
  # java|javascript|typescript|python — harness / api / browser layer jobs follow this
  TESTS_LANG: java
  APP_SLUG: autotests-ai-multistack-app
  # ── Allure paths (literals — keep in sync with TESTS_DIR above) ───────────
  ALLURE_RESULTS_DIR: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results
  ALLURE_REPORT_DIR: tests/java/tests-java-gradle-junit5-allure3-selenide/build/reports/allure-report/allureReport
  ALLURE_AWESOME_DIR: tests/java/tests-java-gradle-junit5-allure3-selenide/build/reports/allure-report/allureReport/awesome
  JAVA_VERSION: "21"
  NODE_VERSION: "26"
  PYTHON_VERSION: "3.12"
  CHECKOUT_FETCH_DEPTH: "1"
  GRADLE_CACHE_READ_ONLY: "false"
  GO_VERSION: "1.25"
  CHROME_FOR_TESTING_VERSION: "148.0.7778.178"
  ALLURECTL_VERSION: "2.22.0"
  ALLURE_JOB_RUN_ID: ${{ inputs.ALLURE_JOB_RUN_ID || '' }}
  ALLURE_JOB_RUN_UID: ${{ github.run_id }}-${{ github.run_attempt }}
  ALLURE_USERNAME: ${{ inputs.ALLURE_USERNAME || '' }}
  # TestOps UI rerun passes ALLURE_JOB_RUN_ID — keep selective testplan.
  # Ordinary PR/push leave this false so -DincludeTags / layer jobs own selection.
  ALLURE_KEEP_TESTPLAN: ${{ github.event_name == 'workflow_dispatch' && inputs.ALLURE_JOB_RUN_ID != '' }}
  # Launch env axes for TestOps filters (real values — not sandbox toy OS/VERSION).
  BROWSER: Chrome
  OS: Linux
  ENDPOINT: prod
  VERSION: ${{ github.sha }}
  BRANCH: ${{ github.head_ref || github.ref_name }}
  SONAR_SCANNER_NPM_VERSION: "5.0.0"
  SONAR_QUALITY_GATE_TIMEOUT: "600"
  # ── Derived (literals — workflow env cannot nest env.*; keep in sync above) ─
  PUBLIC_HOST: autotests.ai
  STACK_MOUNT: stack
  APP_URL: https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
  STAGE_APP_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
  # Playwright → UI_URL; pytest → BASE_URL (same stand URL)
  UI_URL: https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
  BASE_URL: https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
  GHCR_BACKEND_PACKAGE: autotests-ai-multistack-app-backend-java-spring
  GHCR_FRONTEND_PACKAGE: autotests-ai-multistack-app-frontend-typescript-react
  RUN_LABEL: "${{ github.repository }} #${{ github.run_number }}"
  SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL || 'https://sonar.qa.guru' }}
  # ── Non-secret reporting defaults ───────────────────────────────────────
  ALLURE_ENDPOINT: ${{ vars.ALLURE_ENDPOINT || 'https://allure.qa.guru' }}
  ALLURE_PROJECT_ID: ${{ vars.ALLURE_PROJECT_ID }}

  # ── Inline CI helpers (no .github/scripts — logic lives in workflow env) ─
  # Step templates: YAML anchors at first use (&checkout, &setup-java, &setup-gradle,
  # &cache-gradle-cc, &cache-allurectl, &cache-playwright-browsers,
  # &cache-chrome-for-testing, &setup-python, &pip-install, &setup-node, &npm-ci,
  # &cache-allure-nm, &install-playwright, &upload-allure-results, &write-sonar-summary,
  # &docker-login-ghcr, &setup-docker-buildx, &docker-build-push, &ssh-deploy).
  # Job env supplies if-knobs / paths / artifact names as LITERALS — GitHub rejects
  # ${{ env.* }} inside jobs.<id>.env (Unrecognized named-value: 'env'). Keep in sync
  # with BACKEND_* / FRONTEND_* / TESTS_* above. Step if/with may still use env.*.
  # Gradle/test commands stay inline.
  ALLURE_WRITE_ENVIRONMENT: |
    write_allure_environment() {
      if [[ -z "${ALLURE_RESULTS:-}" ]]; then
        echo "write-allure-environment: ALLURE_RESULTS unset — skip"
        return 0
      fi
      mkdir -p "$ALLURE_RESULTS"
      props="${ALLURE_RESULTS}/environment.properties"
      tmp="$(mktemp)"
      for key in BROWSER OS ENDPOINT VERSION BRANCH; do
        val="${!key:-}"
        [[ -n "$val" ]] || continue
        val="${val//$'\n'/ }"
        printf '%s=%s\n' "$key" "$val" >> "$tmp"
      done
      if [[ ! -s "$tmp" ]]; then
        echo "write-allure-environment: no axes set — skip"
        rm -f "$tmp"
        return 0
      fi
      mv "$tmp" "$props"
      echo "Wrote ${props} ($(wc -l <"$props" | tr -d ' ') keys)"
    }
  ALLURECTL_RUN: |
    eval "${ALLURE_WRITE_ENVIRONMENT}"
    run_with_allurectl() {
      if [[ "$#" -eq 0 ]]; then
        echo "usage: run_with_allurectl <command...>" >&2
        return 2
      fi
      run_then_write_env() {
        set +e
        "$@"
        local code=$?
        set -e
        write_allure_environment || echo "warning: failed to write environment.properties (tests exit code unchanged)" >&2
        return "$code"
      }
      fallback() {
        if [[ "${GITHUB_ACTIONS:-false}" == "true" ]]; then
          echo "::warning title=TestOps live upload unavailable::Running tests without allurectl; raw results remain available for fallback."
        else
          echo "TestOps live upload unavailable — running tests without allurectl"
        fi
        run_then_write_env "$@"
        return $?
      }
      if [[ "${ALLURE_LIVE_ENABLED:-false}" != "true" ]] || ! command -v allurectl >/dev/null 2>&1; then
        fallback "$@"
        return $?
      fi
      for name in ALLURE_ENDPOINT ALLURE_TOKEN ALLURE_PROJECT_ID ALLURE_RESULTS ALLURE_LAUNCH_ID ALLURE_JOB_RUN_ID; do
        if [[ -z "${!name:-}" ]]; then
          echo "Missing ${name}"
          fallback "$@"
          return $?
        fi
      done
      echo "Streaming Allure results to launch ${ALLURE_LAUNCH_ID}, job-run ${ALLURE_JOB_RUN_ID}"
      allurectl --http-timeout 1m watch \
        --job-run-child \
        --continue-on-error \
        -- \
        bash -c '
          set -euo pipefail
          eval "${ALLURE_WRITE_ENVIRONMENT}"
          if [[ "${ALLURE_KEEP_TESTPLAN:-false}" != "true" ]]; then
            if [[ -n "${ALLURE_TESTPLAN_PATH:-}" ]]; then
              echo "Ignoring TestOps testplan (${ALLURE_TESTPLAN_PATH}); CI layer filters own the selection"
              rm -f "${ALLURE_TESTPLAN_PATH}"
              unset ALLURE_TESTPLAN_PATH
            fi
            rm -f .allure/testplan.json
          else
            echo "Keeping TestOps testplan (ALLURE_KEEP_TESTPLAN=true)"
          fi
          set +e
          "$@"
          code=$?
          set -e
          write_allure_environment || echo "warning: failed to write environment.properties (tests exit code unchanged)" >&2
          exit "$code"
        ' bash "$@"
    }
  WRITE_SONAR_SUMMARY: |
    {
      echo "## ${GITHUB_JOB}"
      echo ""
      echo "- projectKey: \`${SONAR_PROJECT_KEY}\`"
      echo "- host: \`${SONAR_HOST_URL}\`"
      echo "- dashboard: ${SONAR_HOST_URL%/}/dashboard?id=${SONAR_PROJECT_KEY}"
      echo "- commit: \`${GITHUB_SHA:0:7}\`"
    } >> "$GITHUB_STEP_SUMMARY"
  DOCKER_FRONTEND_BUILD_ARGS: |
    REACT_UI=frontend/_shared/frontend-react-ui
    UI_RUNTIME=frontend/_shared/frontend-javascript-app
  DEPLOY_SSH_SCRIPT: |
    set -euo pipefail
    APP_DIR="${DEPLOY_APP_DIR:-/home/autotests_ai_multistack/autotests-ai-multistack-app}"
    cd "$APP_DIR"
    git fetch origin
    git checkout --force "$IMAGE_TAG"
    echo "$GHCR_TOKEN" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
    COMPOSE="docker compose"
    if [ -n "${DEPLOY_COMPOSE_PROJECT:-}" ]; then
      COMPOSE="$COMPOSE --project-name ${DEPLOY_COMPOSE_PROJECT}"
    fi
    if [ -n "${DEPLOY_COMPOSE_ENV_FILE:-}" ]; then
      COMPOSE="$COMPOSE --env-file ${DEPLOY_COMPOSE_ENV_FILE}"
    fi
    $COMPOSE pull ${DEPLOY_COMPOSE_SERVICES}
    $COMPOSE up -d --remove-orphans ${DEPLOY_COMPOSE_SERVICES}
    if [ -n "${DEPLOY_HEALTH_URL:-}" ]; then
      curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${DEPLOY_HEALTH_URL}"
    fi
    docker logout ghcr.io

jobs:
  # Same flags as dispatch `deploy`. Push infers from paths so tests/ does not
  # rebuild the app; PR is none (no CD). Not a pyramid job.
  # Shared TestOps job-run lives here too (soft: missing creds / outage disable
  # live upload without blocking tests) so the graph is one root:
  # trigger → unit-tests (backend) / component-tests (frontend) / tests-harness.
  trigger:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    outputs:
      backend: ${{ steps.set.outputs.backend }}
      frontend: ${{ steps.set.outputs.frontend }}
      tests: ${{ steps.set.outputs.tests }}
      cd_production: ${{ steps.set.outputs.cd_production }}
      cd_stage: ${{ steps.set.outputs.cd_stage }}
      enabled: ${{ steps.context.outputs.enabled }}
      launch_id: ${{ steps.context.outputs.launch_id }}
      job_run_id: ${{ steps.context.outputs.job_run_id }}
      launch_url: ${{ steps.context.outputs.launch_url }}
    steps:
      - if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Resolve lanes
        id: set
        env:
          BEFORE: ${{ github.event.before }}
          DEPLOY: ${{ github.event.inputs.deploy || '' }}
          DEPLOY_TARGET: ${{ github.event.inputs.deploy_target || '' }}
        run: |
          set -euo pipefail
          backend=false
          frontend=false
          tests=false
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            case "${DEPLOY}" in
              backend) backend=true ;;
              frontend) frontend=true ;;
              tests) tests=true ;;
              all) backend=true; frontend=true; tests=true ;;
              none|'') ;;
              *) echo "unknown deploy=${DEPLOY}" >&2; exit 1 ;;
            esac
          elif [ "${GITHUB_EVENT_NAME}" = "push" ] && { [ "${GITHUB_REF}" = "refs/heads/main" ] || [ "${GITHUB_REF}" = "refs/heads/develop" ]; }; then
            if [ -z "${BEFORE:-}" ] || [ "$BEFORE" = "0000000000000000000000000000000000000000" ] \
              || ! git cat-file -e "${BEFORE}^{commit}" 2>/dev/null; then
              backend=true
              frontend=true
              tests=true
            else
              CHANGED="$(git diff --name-only "$BEFORE" "$GITHUB_SHA")"
              match() { printf '%s\n' "$CHANGED" | grep -qE "$1"; }
              SHARED='deploy/|docker-compose|\.github/workflows/ci\.yml'
              if match "^(backend/|${SHARED})"; then backend=true; fi
              if match "^(frontend/|${SHARED})"; then frontend=true; fi
              if match "^(tests/|${SHARED})"; then tests=true; fi
            fi
          fi
          cd_production=false
          cd_stage=false
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            case "${DEPLOY_TARGET}" in
              stage) cd_stage=true ;;
              both|production|'')
                cd_production=true
                cd_stage=true
                ;;
              *) echo "unknown deploy_target=${DEPLOY_TARGET}" >&2; exit 1 ;;
            esac
            if [ "${GITHUB_REF}" != "refs/heads/main" ]; then
              cd_production=false
              if [ "${DEPLOY_TARGET}" = "production" ] || [ -z "${DEPLOY_TARGET}" ]; then
                cd_stage=false
              fi
            fi
          elif [ "${GITHUB_EVENT_NAME}" = "push" ] && [ "${GITHUB_REF}" = "refs/heads/main" ]; then
            cd_production=true
            cd_stage=true
          elif [ "${GITHUB_EVENT_NAME}" = "push" ] && [ "${GITHUB_REF}" = "refs/heads/develop" ]; then
            cd_stage=true
          fi
          printf 'backend=%s\nfrontend=%s\ntests=%s\ncd_production=%s\ncd_stage=%s\n' \
            "$backend" "$frontend" "$tests" "$cd_production" "$cd_stage" >> "$GITHUB_OUTPUT"
          echo "trigger backend=${backend} frontend=${frontend} tests=${tests} cd_production=${cd_production} cd_stage=${cd_stage}"

      - name: Check TestOps configuration
        id: configured
        env:
          ALLURE_TOKEN: ${{ secrets.ALLURE_TOKEN }}
        run: |
          if [ -n "${ALLURE_TOKEN:-}" ] && [ -n "${ALLURE_PROJECT_ID:-}" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "ALLURE_LIVE_ENABLED=true" >> "$GITHUB_ENV"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "TestOps credentials unavailable — live upload disabled"
          fi

      # Binary only (not token / launch). setup-allurectl@v1 stores under
      # $RUNNER_TOOL_CACHE/allurectl/<ver>/<goarch>/ + sibling `.complete` and
      # always hits GitHub API, so we pin the release into $RUNNER_TEMP/allurectl-bin.
      - &cache-allurectl
        name: Cache allurectl binary
        if: always() && env.ALLURE_LIVE_ENABLED == 'true'
        id: cache-allurectl
        continue-on-error: true
        timeout-minutes: 1
        uses: actions/cache@v6
        with:
          path: ${{ runner.temp }}/allurectl-bin
          key: allurectl-${{ runner.os }}-${{ runner.arch }}-${{ env.ALLURECTL_VERSION }}

      - &setup-live-allurectl
        name: Install pinned allurectl for live upload
        if: always() && env.ALLURE_LIVE_ENABLED == 'true'
        id: setup-allurectl
        continue-on-error: true
        timeout-minutes: 1
        env:
          ALLURE_TOKEN: ${{ secrets.ALLURE_TOKEN }}
        run: |
          set -euo pipefail
          DEST="${RUNNER_TEMP}/allurectl-bin"
          BIN="${DEST}/allurectl"
          mkdir -p "$DEST"
          bin_ok() { [[ -x "$BIN" ]] && "$BIN" --version >/dev/null 2>&1; }
          if bin_ok; then
            echo "allurectl ${ALLURECTL_VERSION} restored from cache (${BIN})"
          else
            rm -f "$BIN"
            case "${RUNNER_OS}" in Linux) os=linux ;; macOS) os=darwin ;; *)
              echo "unsupported RUNNER_OS=${RUNNER_OS}" >&2; exit 1 ;; esac
            case "${RUNNER_ARCH}" in X64) arch=amd64 ;; ARM64) arch=arm64 ;; *)
              echo "unsupported RUNNER_ARCH=${RUNNER_ARCH}" >&2; exit 1 ;; esac
            url="https://github.com/allure-framework/allurectl/releases/download/${ALLURECTL_VERSION}/allurectl_${os}_${arch}"
            echo "Downloading ${url}"
            curl -fsSL --connect-timeout 10 --max-time 45 -o "$BIN" "$url"
            chmod +x "$BIN"
            bin_ok || { echo "downloaded allurectl is not executable" >&2; exit 1; }
            "$BIN" --version
          fi
          echo "$DEST" >> "$GITHUB_PATH"
          if [[ -n "${ALLURE_TOKEN:-}" ]]; then
            echo "ALLURE_TOKEN=${ALLURE_TOKEN}" >> "$GITHUB_ENV"
          fi
          if [[ -z "${ALLURE_JOB_UID:-}" ]]; then
            echo "ALLURE_JOB_UID=${GITHUB_REPOSITORY}/actions/workflows/ci.yml" >> "$GITHUB_ENV"
          fi

      - &cache-playwright-browsers
        name: Cache Playwright browsers
        if: env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript'
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          # hashFiles needs literal globs — keep in sync when flipping TESTS_DIR.
          key: playwright-${{ runner.os }}-${{ hashFiles('tests/**/package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Create or adopt shared TestOps context
        id: context
        if: always()
        env:
          ALLURE_LAUNCH_NAME: "${{ env.APP_SLUG }} CI — ${{ env.RUN_LABEL }}"
        run: |
          set +e
          set -uo pipefail

          emit_disabled() {
            local cleanup_job_run_id="${1:-}"
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "launch_id=" >> "$GITHUB_OUTPUT"
            echo "job_run_id=${cleanup_job_run_id}" >> "$GITHUB_OUTPUT"
            echo "launch_url=" >> "$GITHUB_OUTPUT"
          }

          if [ "${{ steps.configured.outputs.enabled }}" != "true" ] ||
             [ "${{ steps.setup-allurectl.outcome }}" != "success" ] ||
             ! command -v allurectl >/dev/null 2>&1; then
            emit_disabled
            exit 0
          fi

          JOB_RUN_ID="${ALLURE_JOB_RUN_ID:-}"
          START_OUT=""
          if [ -z "$JOB_RUN_ID" ]; then
            JOB_RUN_BRANCH="${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
            START_OUT="$(
              allurectl --http-timeout 10s job-run start \
                --project-id "$ALLURE_PROJECT_ID" \
                --ci-type github \
                --ci-endpoint "$GITHUB_SERVER_URL" \
                --job-name "$GITHUB_WORKFLOW" \
                --job-uid "$ALLURE_JOB_UID" \
                --job-url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/workflows/ci.yml" \
                --job-run-name "#$GITHUB_RUN_NUMBER.$GITHUB_RUN_ATTEMPT" \
                --job-run-uid "$ALLURE_JOB_RUN_UID" \
                --job-run-url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
                --job-run-branch "$JOB_RUN_BRANCH" \
                --launch-name "$ALLURE_LAUNCH_NAME"
            )"
            START_EXIT=$?
            if [ "$START_EXIT" -ne 0 ]; then
              echo "Unable to create TestOps job-run — live upload disabled"
              emit_disabled
              exit 0
            fi
          fi

          resolve_job_run_env() {
            allurectl --http-timeout 10s job-run env \
              --project-id "$ALLURE_PROJECT_ID" \
              "$@"
          }

          if [ -n "$JOB_RUN_ID" ]; then
            JOB_ENV="$(resolve_job_run_env --job-run-id "$JOB_RUN_ID")"
          else
            JOB_ENV="$(resolve_job_run_env --job-run-uid "$ALLURE_JOB_RUN_UID")"
          fi
          ENV_EXIT=$?
          if [ "$ENV_EXIT" -ne 0 ] && [ -n "$START_OUT" ]; then
            JOB_RUN_ID="$(
              printf '%s' "$START_OUT" \
                | grep -Eo 'Jobrun\[[0-9]+\]' \
                | head -1 \
                | sed -E 's/Jobrun\[([0-9]+)\]/\1/'
            )"
            if [ -n "$JOB_RUN_ID" ]; then
              JOB_ENV="$(resolve_job_run_env --job-run-id "$JOB_RUN_ID")"
              ENV_EXIT=$?
            fi
          fi
          if [ "$ENV_EXIT" -ne 0 ]; then
            echo "Unable to resolve TestOps job-run context — live upload disabled"
            emit_disabled "${JOB_RUN_ID:-}"
            exit 0
          fi

          # `job-run env` is the official shell-assignment handoff contract.
          # Export it locally, then expose only non-secret numeric IDs and URL.
          # shellcheck disable=SC2046
          export $(printf '%s\n' "$JOB_ENV")
          if ! [[ "${ALLURE_JOB_RUN_ID:-}" =~ ^[0-9]+$ ]] ||
             ! [[ "${ALLURE_LAUNCH_ID:-}" =~ ^[0-9]+$ ]]; then
            echo "Invalid TestOps context IDs — live upload disabled"
            emit_disabled "$JOB_RUN_ID"
            exit 0
          fi

          LAUNCH_URL="${ALLURE_LAUNCH_URL:-${ALLURE_ENDPOINT%/}/launch/${ALLURE_LAUNCH_ID}}"
          echo "enabled=true" >> "$GITHUB_OUTPUT"
          echo "launch_id=${ALLURE_LAUNCH_ID}" >> "$GITHUB_OUTPUT"
          echo "job_run_id=${ALLURE_JOB_RUN_ID}" >> "$GITHUB_OUTPUT"
          echo "launch_url=${LAUNCH_URL}" >> "$GITHUB_OUTPUT"

  # Main owns the stage host: cancel develop CI so promotion is not tested
  # against a WIP checkout. Does not `needs` deploy — runs in parallel with build.
  preempt-develop-cd:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    permissions:
      actions: write
    steps:
      - name: Cancel develop CI
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          ids=""
          for status in in_progress queued pending waiting; do
            ids="$ids $(gh run list --repo "$GITHUB_REPOSITORY" --workflow CI --branch develop --status "$status" --limit 20 --json databaseId --jq '.[].databaseId')"
          done
          for id in $ids; do
            echo "preempt develop run $id"
            gh run cancel "$id" --repo "$GITHUB_REPOSITORY" || true
          done

  # Develop must not start (or continue) if main already claimed the stage.
  yield-to-main-cd:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/develop'
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    permissions:
      actions: write
    steps:
      - name: Abort this run when main CD is queued or running
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          main_busy() {
            for status in in_progress queued pending waiting; do
              count="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow CI --branch main --status "$status" --limit 5 --json databaseId --jq 'length')"
              if [ "$count" != "0" ]; then
                return 0
              fi
            done
            return 1
          }
          if main_busy || { sleep 3; main_busy; }; then
            echo "main CD owns the stage — cancelling this develop run"
            gh run cancel "$GITHUB_RUN_ID" --repo "$GITHUB_REPOSITORY" || true
          fi

  # Keep the backend root first: GitHub has no explicit graph coordinates and uses
  # declaration order as a layout hint for otherwise independent jobs.
  unit-tests:
    needs: trigger
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      LAYER: unit
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CC_IF: "true"
      GRADLE_CC_MODULE: backend/java/backend-java-spring
      GRADLE_CC_KEY_PREFIX: backend-java-spring
      ALLURE_RESULTS_ARTIFACT: allure-results-unit
      ALLURE_RESULTS_UPLOAD_PATH: backend/java/backend-java-spring/build/allure-results/**
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: backend/java/backend-java-spring/package-lock.json
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: backend/java/backend-java-spring/requirements.txt
      PYTHON_WORKDIR: backend/java/backend-java-spring
      NPM_CI_IF: "false"
      NPM_WORKDIR: backend/java/backend-java-spring
    steps:
      - &checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: ${{ env.CHECKOUT_FETCH_DEPTH }}

      - *cache-allurectl
      - *setup-live-allurectl

      # ── java / kotlin (Gradle + JaCoCo) ───────────────────────────────────
      # setup-java must not set `cache: gradle` — setup-gradle owns ~/.gradle.
      # cache-encryption-key exports GRADLE_ENCRYPTION_KEY so CC is reusable across runners.
      - &setup-java
        if: env.SETUP_JVM_IF == 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
      - &setup-gradle
        if: env.SETUP_JVM_IF == 'true'
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: ${{ env.GRADLE_CACHE_READ_ONLY == 'true' }}
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
        # GUH key is ${protocol}|${os}|${job-id}[…]. v6 has no cache-key-prefix input.
        # Tests-module readers set GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
        # (action env, not workflow-job-context) so restore prefers harness GUH.
      # hashFiles needs literal globs (format(env.*) is rejected at workflow parse).
      # Keep in sync when swapping BACKEND_DIR / TESTS_DIR in workflow env.
      - &cache-gradle-cc
        if: env.GRADLE_CC_IF == 'true'
        uses: actions/cache@v6
        with:
          path: ${{ env.GRADLE_CC_MODULE }}/.gradle/configuration-cache
          key: gradle-cc-${{ env.GRADLE_CC_KEY_PREFIX }}-${{ hashFiles('backend/java/backend-java-spring/*.gradle*', 'backend/java/backend-java-spring/gradle/wrapper/gradle-wrapper.properties', 'backend/java/backend-java-spring/gradle.properties', 'backend/kotlin/backend-kotlin-spring/*.gradle*', 'backend/kotlin/backend-kotlin-spring/gradle/wrapper/gradle-wrapper.properties', 'tests/java/tests-java-gradle-junit5-allure3-selenide/*.gradle*', 'tests/java/tests-java-gradle-junit5-allure3-selenide/gradle/wrapper/gradle-wrapper.properties', 'tests/java/tests-java-gradle-junit5-allure3-selenide/gradle.properties') }}-${{ github.sha }}
          restore-keys: |
            gradle-cc-${{ env.GRADLE_CC_KEY_PREFIX }}-
      - name: Run unit tests (Gradle + JaCoCo)
        if: env.BACKEND_LANG == 'java' || env.BACKEND_LANG == 'kotlin'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.BACKEND_DIR }}/build/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test jacocoTestReport jacocoTestCoverageVerification -DexcludeTags=integration
        working-directory: ${{ env.BACKEND_DIR }}
      - if: (env.BACKEND_LANG == 'java' || env.BACKEND_LANG == 'kotlin') && steps.run-tests.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-backend
          path: |
            ${{ env.BACKEND_DIR }}/build/jacoco/**
            ${{ env.BACKEND_DIR }}/build/reports/jacoco/**
            ${{ env.BACKEND_DIR }}/build/classes/**
          if-no-files-found: error
      # ── python ───────────────────────────────────────────────────────────
      - &setup-python
        if: env.SETUP_PYTHON_IF == 'true'
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: ${{ env.PYTHON_CACHE_DEPENDENCY_PATH }}
      - &pip-install
        name: Install Python dependencies
        if: env.SETUP_PYTHON_IF == 'true'
        run: python -m pip install -r requirements.txt
        working-directory: ${{ env.PYTHON_WORKDIR }}
      - name: Run Python tests (pytest)
        if: env.BACKEND_LANG == 'python'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.BACKEND_DIR }}/build/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl python -m pytest
        working-directory: ${{ env.BACKEND_DIR }}

      # ── go ───────────────────────────────────────────────────────────────
      - if: env.BACKEND_LANG == 'go'
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: ${{ env.BACKEND_DIR }}/go.sum
      - name: Run Go unit tests
        if: env.BACKEND_LANG == 'go'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.BACKEND_DIR }}/build/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl go test ./...
        working-directory: ${{ env.BACKEND_DIR }}

      # ── javascript / typescript ──────────────────────────────────────────
      - &setup-node
        if: env.SETUP_NODE_IF == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: ${{ env.NODE_CACHE_DEPENDENCY_PATH }}
      # Teaching: npm ci installs the lockfile tree. After bumping package.json
      # pins, run `nvm use 26 && npm install --package-lock-only` and commit both.
      - &npm-ci
        name: Install npm dependencies
        if: env.NPM_CI_IF == 'true'
        run: npm ci
        working-directory: ${{ env.NPM_WORKDIR }}
      - name: Run backend unit tests (npm)
        if: env.BACKEND_LANG == 'javascript' || env.BACKEND_LANG == 'typescript'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.BACKEND_DIR }}/build/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test
        working-directory: ${{ env.BACKEND_DIR }}
      - &upload-allure-results
        name: Publish raw Allure results
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ALLURE_RESULTS_ARTIFACT }}
          path: ${{ env.ALLURE_RESULTS_UPLOAD_PATH }}
          if-no-files-found: ignore
          retention-days: 1

  integration-tests:
    needs: [unit-tests, trigger]
    if: >-
      always() && !cancelled() && needs.unit-tests.result == 'success' && (
        github.event_name == 'pull_request' ||
        (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')) ||
        (github.event_name == 'workflow_dispatch' && inputs.run_integration)
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      LAYER: integration
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CC_IF: "true"
      GRADLE_CC_MODULE: backend/java/backend-java-spring
      GRADLE_CC_KEY_PREFIX: backend-java-spring-integration
      ALLURE_RESULTS_ARTIFACT: allure-results-integration
      ALLURE_RESULTS_UPLOAD_PATH: backend/java/backend-java-spring/build/allure-results/**
    steps:
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      # ── java / kotlin (Spring Boot + Testcontainers PostgreSQL) ───────────
      - *setup-java
      - *setup-gradle
      - *cache-gradle-cc
      - name: Run integration tests (Gradle + Testcontainers)
        if: env.BACKEND_LANG == 'java' || env.BACKEND_LANG == 'kotlin'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.BACKEND_DIR }}/build/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -DincludeTags=integration
        working-directory: ${{ env.BACKEND_DIR }}
      - *upload-allure-results

  # Same teaching knob as deploy/sonar: only FRONTEND_DIR. Sibling frontends are not gated.
  # Coverage always on (this job is the deploy default). lint/typecheck --if-present for JS.
  # Do not point FRONTEND at frontend-javascript-vanilla (no npm/Vitest — browser layers only).
  component-tests:
    needs: trigger
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      LAYER: component
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_NODE_IF: "true"
      NODE_CACHE_DEPENDENCY_PATH: frontend/typescript/frontend-typescript-react/package-lock.json
      NPM_CI_IF: "true"
      NPM_WORKDIR: frontend/typescript/frontend-typescript-react
      ALLURE_RESULTS_ARTIFACT: allure-results-component
      ALLURE_RESULTS_UPLOAD_PATH: frontend/typescript/frontend-typescript-react/allure-results/**
    steps:
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl
      - *setup-node
      - *npm-ci
      - name: Lint frontend
        run: npm run lint --if-present
        working-directory: ${{ env.FRONTEND_DIR }}
      - name: Typecheck frontend
        run: npm run typecheck --if-present
        working-directory: ${{ env.FRONTEND_DIR }}
      - name: Run component tests (Vitest + coverage)
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.FRONTEND_DIR }}/allure-results
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test -- --coverage
        working-directory: ${{ env.FRONTEND_DIR }}
      - if: steps.run-tests.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-frontend
          path: ${{ env.FRONTEND_DIR }}/coverage/
          if-no-files-found: error
      - *upload-allure-results

  # Stub API on runner (compose --profile mock); after jsdom, before FE image push.
  ui-mock-tests:
    needs: [trigger, component-tests]
    if: >-
      always() && !cancelled() &&
      needs.component-tests.result == 'success' && (
        github.event_name == 'pull_request' ||
        needs.trigger.outputs.frontend == 'true' ||
        (github.event_name == 'workflow_dispatch' &&
         (inputs.run_mock || inputs.update_mock_screenshots))
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
      actions: write
    env:
      LAYER: ui-mock
      SCREENSHOT_OS: linux
      SCREENSHOT_BROWSER: chrome
      # Stable GHA BuildKit scope — hashFiles is not allowed in job env; layers are content-addressed.
      DOCKER_CACHE_SCOPE: autotests-ai-multistack-app-docker-mock-stand-gateway
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      # Share tests-module GUH with tests-harness (setup-gradle@v6 keys on github.job).
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      SETUP_BUILDX_IF: "true"
      # Build path. Do not name this DOCKER_CONTEXT — GHA injects job env into
      # every step; Docker CLI/buildx treat DOCKER_CONTEXT as a named context.
      DOCKER_BUILD_CONTEXT: .
      DOCKER_FILE: frontend/typescript/frontend-typescript-react/Dockerfile
      DOCKER_TAGS: ghcr.io/${{ github.repository_owner }}/autotests-ai-multistack-app-frontend-typescript-react:mock-local
      DOCKER_BUILD_ARGS: |
        REACT_UI=frontend/_shared/frontend-react-ui
        UI_RUNTIME=frontend/_shared/frontend-javascript-app
      DOCKER_OCI_LABELS: |
        org.opencontainers.image.title=autotests-ai-multistack-app-frontend-typescript-react
        org.opencontainers.image.description=autotests-ai-multistack-app · frontend-typescript-react
        org.opencontainers.image.source=https://github.com/autotests-ai/autotests-ai-multistack-app
        org.opencontainers.image.url=https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
        org.opencontainers.image.vendor=autotests-ai
      ALLURE_RESULTS_ARTIFACT: allure-results-ui-mock
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      # ── java (-Denv=mock -DincludeTags=mock) ────────────────────
      # Read-only GUH (GRADLE_CACHE_READ_ONLY). No CC restore: entries pin
      # absolute GRADLE_USER_HOME (JaCoCo agent jars) from other runners.
      - *setup-java
      - *setup-gradle
      # mock has no remoteUrl: the stand lives on the runner's localhost, which a
      # Selenoid browser could not reach, so the browser has to be local too.
      # setup-chrome only has an in-runner tool-cache; persist it like allurectl.
      # Path is $RUNNER_TOOL_CACHE/setup-chrome/{chrome,chromedriver}/<ver>/<arch>/.
      - &cache-chrome-for-testing
        name: Cache Chrome for Testing
        if: env.TESTS_LANG == 'java'
        id: cache-chrome-for-testing
        continue-on-error: true
        uses: actions/cache@v6
        with:
          path: ${{ runner.tool_cache }}/setup-chrome
          key: chrome-for-testing-${{ runner.os }}-${{ runner.arch }}-${{ env.CHROME_FOR_TESTING_VERSION }}
      - name: Install pinned Chrome for Testing
        if: env.TESTS_LANG == 'java'
        id: setup-chrome
        uses: browser-actions/setup-chrome@v2.1.2
        with:
          chrome-version: ${{ env.CHROME_FOR_TESTING_VERSION }}
          install-chromedriver: true
      # stand-gateway serves the SPA and proxies /api/ to WireMock stubs (deploy/mock/mappings).
      # Pre-build frontend with GHA layer cache; compose up pulls api-mock and starts nginx gateway.
      - &setup-docker-buildx
        if: env.SETUP_BUILDX_IF != 'false'
        uses: docker/setup-buildx-action@v4
      - &docker-build-push
        if: env.SETUP_BUILDX_IF != 'false'
        uses: docker/build-push-action@v7
        with:
          context: ${{ env.DOCKER_BUILD_CONTEXT }}
          file: ${{ env.DOCKER_FILE }}
          tags: ${{ env.DOCKER_TAGS }}
          build-args: ${{ env.DOCKER_BUILD_ARGS }}
          labels: ${{ env.DOCKER_OCI_LABELS }}
          load: true
          cache-from: type=gha,scope=${{ env.DOCKER_CACHE_SCOPE }}
          cache-to: type=gha,mode=max,scope=${{ env.DOCKER_CACHE_SCOPE }}
      - name: Bring up mock stand
        if: env.TESTS_LANG == 'java'
        run: |
          set -euo pipefail
          docker compose --profile mock up -d stand-gateway
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors \
            "http://127.0.0.1:${MOCK_GATEWAY_PORT}/api/health"
        env:
          IMAGE_TAG: mock-local
          MOCK_GATEWAY_PORT: "9911"
      - name: Run ui-mock tests (Gradle)
        if: env.TESTS_LANG == 'java'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          CHROME_BINARY_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          CHROMEDRIVER_PATH: ${{ steps.setup-chrome.outputs.chromedriver-path }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -Denv=mock -DincludeTags=mock
        working-directory: ${{ env.TESTS_DIR }}
      - name: Stash mock Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-mock-stash"
          SRC="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$STASH"
          if [ -d "$SRC" ]; then cp -R "$SRC/." "$STASH/"; fi
      # Same mock compose stand; separate Gradle fail from includeTags=mock.
      # includeTags CSV is OR here — do not combine mock,screenshot in one step.
      # Compare and rewrite are mutually exclusive: a UI change would fail compare
      # and block the rewrite.
      - name: Run screenshot mock compare (Gradle)
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          (github.event_name != 'workflow_dispatch' || inputs.update_mock_screenshots != true)
        id: run-screenshot-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          CHROME_BINARY_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          CHROMEDRIVER_PATH: ${{ steps.setup-chrome.outputs.chromedriver-path }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -Denv=mock -DincludeTags=screenshot
        working-directory: ${{ env.TESTS_DIR }}
      - name: Update screenshots
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_mock_screenshots
        id: update-screenshots
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          CHROME_BINARY_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          CHROMEDRIVER_PATH: ${{ steps.setup-chrome.outputs.chromedriver-path }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -Denv=mock -DincludeTags=screenshot -DupdateScreenshots=true
        working-directory: ${{ env.TESTS_DIR }}
      - name: Merge Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-mock-stash"
          DEST="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$DEST"
          if [ -d "$STASH" ]; then cp -R "$STASH/." "$DEST/"; fi
      - name: Tear down mock stand
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: docker compose --profile mock down -v --remove-orphans
      - *upload-allure-results
      - name: Publish mock screenshot PNGs
        if: >-
          always() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_mock_screenshots
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: mock-screenshot-pngs
          path: ${{ env.TESTS_DIR }}/src/test/resources/screenshots/
          if-no-files-found: ignore

  # Not a pyramid layer and not TestOps: JaCoCo + sonar-tests only.
  # Backend-only → ConfigReader. Frontend/tests/all → full harness (UI tests read ConfigReader).
  # Writer of tests-module GUH; api/e2e/ui-mock/manual restore it via GRADLE_BUILD_ACTION_CACHE_KEY_JOB.
  tests-harness:
    needs: [trigger]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    outputs:
      slice: ${{ steps.slice.outputs.slice }}
    env:
      LAYER: harness
      SETUP_JVM_IF: "true"
      GRADLE_CC_IF: "true"
      GRADLE_CC_MODULE: tests/java/tests-java-gradle-junit5-allure3-selenide
      GRADLE_CC_KEY_PREFIX: tests-java-gradle-junit5-allure3-selenide-${{ github.job }}
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "false"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      PLAYWRIGHT_IF: "false"
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/requirements.txt
      PYTHON_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
    steps:
      - *checkout
      - name: Select harness slice
        id: slice
        env:
          LANE_BACKEND: ${{ needs.trigger.outputs.backend }}
          LANE_FRONTEND: ${{ needs.trigger.outputs.frontend }}
          LANE_TESTS: ${{ needs.trigger.outputs.tests }}
        run: |
          set -euo pipefail
          slice=all
          tags=harness
          if [ "$LANE_BACKEND" = "true" ] && [ "$LANE_FRONTEND" != "true" ] && [ "$LANE_TESTS" != "true" ]; then
            slice=backend
            tags=harness-backend
          fi
          echo "slice=${slice}" >> "$GITHUB_OUTPUT"
          echo "HARNESS_INCLUDE_TAGS=${tags}" >> "$GITHUB_ENV"
          echo "Harness slice=${slice} includeTags=${tags}"

      # ── java (Gradle tags + JaCoCo gate) ──────────────────────────────────
      - *setup-java
      - *setup-gradle
      - *cache-gradle-cc
      # Coverage gate reads build/jacoco/test.exec, so it is only meaningful with this filter.
      # Not streamed to TestOps (helpers, not product cases).
      - name: Run harness tests (Gradle)
        if: env.TESTS_LANG == 'java'
        id: run-tests
        run: ./gradlew test jacocoTestReport jacocoTestCoverageVerification -Denv=ci -DincludeTags="${HARNESS_INCLUDE_TAGS}"
        working-directory: ${{ env.TESTS_DIR }}
      - if: env.TESTS_LANG == 'java' && steps.run-tests.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-harness
          path: |
            ${{ env.TESTS_DIR }}/build/jacoco/**
            ${{ env.TESTS_DIR }}/build/reports/jacoco/**
            ${{ env.TESTS_DIR }}/build/classes/**
          if-no-files-found: error
      # ── javascript / typescript (full suite; no tag pyramid yet) ──────────
      - *setup-node
      - *npm-ci
      - *cache-playwright-browsers
      - &install-playwright
        name: Install Playwright Chromium
        if: env.PLAYWRIGHT_IF == 'true'
        run: npx playwright install --with-deps chromium
        working-directory: ${{ env.TESTS_DIR }}
      - name: Run tests (npm)
        if: >-
          steps.slice.outputs.slice != 'backend' &&
          (env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript')
        run: npm test
        working-directory: ${{ env.TESTS_DIR }}

      # ── python (full suite; no tag pyramid yet) ───────────────────────────
      - *setup-python
      - *pip-install
      - name: Run Python tests (pytest)
        if: steps.slice.outputs.slice != 'backend' && env.TESTS_LANG == 'python'
        run: python -m pytest
        working-directory: ${{ env.TESTS_DIR }}

  # Quality axis (SonarQube) — hard gate before deploy (not build). Coverage reused via artifacts.
  # Quality Gate wait is native in each scanner (`sonar.qualitygate.wait=true`).
  sonar-backend:
    needs: [unit-tests, integration-tests]
    if: >-
      !cancelled() &&
      needs.unit-tests.result == 'success' &&
      (needs.integration-tests.result == 'success' || needs.integration-tests.result == 'skipped')
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      SONAR_PROJECT_KEY: ${{ vars.SONAR_BACKEND_PROJECT_KEY || 'autotests-ai-multistack-app-backend-java-spring' }}
      CHECKOUT_FETCH_DEPTH: "0"
      SETUP_JVM_IF: "true"
      GRADLE_CC_IF: "true"
      GRADLE_CC_MODULE: backend/java/backend-java-spring
      GRADLE_CC_KEY_PREFIX: backend-java-spring-sonar
    steps:
      - *checkout
      - *setup-java
      - *setup-gradle
      - *cache-gradle-cc
      - uses: actions/download-artifact@v8
        with:
          name: coverage-backend
          path: ${{ env.BACKEND_DIR }}/build
      - name: Sonar scan + gate (backend)
        working-directory: ${{ env.BACKEND_DIR }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          if [[ ! -f build/reports/jacoco/test/jacocoTestReport.xml ]]; then
            echo "ERROR: JaCoCo report missing — unit-tests artifact incomplete" >&2
            exit 1
          fi

          if [[ -z "${SONAR_TOKEN:-}" ]]; then
            echo "ERROR: SONAR_TOKEN unset" >&2
            exit 1
          fi

          echo "==> sonar scan (coverage artifact) → ${SONAR_HOST_URL} (${SONAR_PROJECT_KEY})"
          # Token via env only (SONAR_TOKEN) — never argv (canon: docs/sonar/GITHUB-ACTIONS.md).
          ./gradlew sonar --no-daemon \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.projectKey="${SONAR_PROJECT_KEY}" \
            -Dsonar.projectName="${SONAR_PROJECT_KEY}" \
            -Dsonar.qualitygate.wait=true \
            -Dsonar.qualitygate.timeout="${SONAR_QUALITY_GATE_TIMEOUT}"
      - &write-sonar-summary
        name: Write Sonar summary
        if: always()
        run: |
          eval "${WRITE_SONAR_SUMMARY}"
      - name: Upload Sonar report-task
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: sonar-backend-report-task
          path: |
            ${{ env.BACKEND_DIR }}/build/sonar/report-task.txt
            ${{ env.BACKEND_DIR }}/.scannerwork/report-task.txt
          if-no-files-found: ignore
          retention-days: 1

  # Tests-module Sonar after full harness. Backend-only is ConfigReader coverage
  # only — skip rather than fail the tests-module gate.
  sonar-tests:
    needs: [tests-harness]
    if: >-
      !cancelled() &&
      needs.tests-harness.result == 'success' &&
      needs.tests-harness.outputs.slice != 'backend'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      SONAR_PROJECT_KEY: ${{ vars.SONAR_TESTS_PROJECT_KEY || 'autotests-ai-multistack-app-tests-java-gradle-junit5-allure3-selenide' }}
      CHECKOUT_FETCH_DEPTH: "0"
      SETUP_JVM_IF: "true"
      GRADLE_CC_IF: "true"
      GRADLE_CC_MODULE: tests/java/tests-java-gradle-junit5-allure3-selenide
      GRADLE_CC_KEY_PREFIX: tests-java-gradle-junit5-allure3-selenide-${{ github.job }}
    steps:
      - *checkout
      - *setup-java
      - *setup-gradle
      - *cache-gradle-cc
      - uses: actions/download-artifact@v8
        with:
          name: coverage-harness
          path: harness
      - name: Sonar scan + gate (tests)
        working-directory: ${{ env.TESTS_DIR }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          JACOCO_XML="${GITHUB_WORKSPACE}/harness/reports/jacoco/test/jacocoTestReport.xml"
          if [[ ! -f "$JACOCO_XML" ]]; then
            echo "ERROR: JaCoCo report missing: $JACOCO_XML" >&2
            exit 1
          fi

          ./gradlew compileTestJava --no-daemon

          if [[ -z "${SONAR_TOKEN:-}" ]]; then
            echo "ERROR: SONAR_TOKEN unset" >&2
            exit 1
          fi

          echo "==> sonar scan (harness coverage artifact) → ${SONAR_HOST_URL} (${SONAR_PROJECT_KEY})"
          ./gradlew sonar --no-daemon \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.projectKey="${SONAR_PROJECT_KEY}" \
            -Dsonar.projectName="${SONAR_PROJECT_KEY}" \
            -Dsonar.coverage.jacoco.xmlReportPaths="${JACOCO_XML}" \
            -Dsonar.qualitygate.wait=true \
            -Dsonar.qualitygate.timeout="${SONAR_QUALITY_GATE_TIMEOUT}"
      - *write-sonar-summary

  sonar-frontend:
    needs: [component-tests, ui-mock-tests]
    if: >-
      !cancelled() &&
      needs.component-tests.result == 'success' &&
      (needs.ui-mock-tests.result == 'success' || needs.ui-mock-tests.result == 'skipped')
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    env:
      SONAR_PROJECT_KEY: ${{ vars.SONAR_FRONTEND_PROJECT_KEY || 'autotests-ai-multistack-app-frontend-typescript-react' }}
      CHECKOUT_FETCH_DEPTH: "0"
      SETUP_NODE_IF: "true"
      NODE_CACHE_DEPENDENCY_PATH: frontend/typescript/frontend-typescript-react/package-lock.json
    steps:
      - *checkout
      - *setup-node
      - uses: actions/download-artifact@v8
        with:
          name: coverage-frontend
          path: ${{ env.FRONTEND_DIR }}/coverage
      - name: Sonar scan + gate (frontend)
        working-directory: ${{ env.FRONTEND_DIR }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          if [[ ! -d coverage ]] || [ -z "$(ls -A coverage 2>/dev/null || true)" ]; then
            echo "ERROR: coverage artifact missing — component-tests incomplete" >&2
            exit 1
          fi
          npm ci

          if [[ -z "${SONAR_TOKEN:-}" ]]; then
            echo "ERROR: SONAR_TOKEN unset" >&2
            exit 1
          fi

          echo "==> sonar scan (coverage artifact) → ${SONAR_HOST_URL} (${SONAR_PROJECT_KEY})"
          npx --yes "@sonar/scan@${SONAR_SCANNER_NPM_VERSION}" \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.projectKey="${SONAR_PROJECT_KEY}" \
            -Dsonar.projectName="${SONAR_PROJECT_KEY}" \
            -Dsonar.qualitygate.wait=true \
            -Dsonar.qualitygate.timeout="${SONAR_QUALITY_GATE_TIMEOUT}"
      - *write-sonar-summary

  build-backend:
    needs: [unit-tests, integration-tests, trigger]
    if: >-
      !cancelled() &&
      needs.unit-tests.result == 'success' &&
      (needs.integration-tests.result == 'success' || needs.integration-tests.result == 'skipped') &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') &&
      needs.trigger.outputs.backend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
      actions: write
    env:
      DOCKER_CACHE_SCOPE: autotests-ai-multistack-app-docker-backend-java-spring
      DOCKER_BUILD_CONTEXT: backend/java/backend-java-spring
      DOCKER_FILE: backend/java/backend-java-spring/Dockerfile
      DOCKER_TAGS: ghcr.io/${{ github.repository_owner }}/autotests-ai-multistack-app-backend-java-spring:${{ github.sha }}
      DOCKER_BUILD_ARGS: ""
      DOCKER_OCI_LABELS: |
        org.opencontainers.image.title=autotests-ai-multistack-app-backend-java-spring
        org.opencontainers.image.description=autotests-ai-multistack-app · backend-java-spring
        org.opencontainers.image.source=https://github.com/autotests-ai/autotests-ai-multistack-app
        org.opencontainers.image.url=https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
        org.opencontainers.image.vendor=autotests-ai
    steps:
      - *checkout
      - &docker-login-ghcr
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - *setup-docker-buildx
      - *docker-build-push
      - name: Push backend Docker image to GHCR
        run: docker compose push ${{ env.BACKEND }}

  build-frontend:
    needs: [ui-mock-tests, trigger]
    if: >-
      !cancelled() &&
      needs.ui-mock-tests.result == 'success' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') &&
      needs.trigger.outputs.frontend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
      actions: write
    env:
      DOCKER_CACHE_SCOPE: autotests-ai-multistack-app-docker-frontend-typescript-react
      DOCKER_BUILD_CONTEXT: .
      DOCKER_FILE: frontend/typescript/frontend-typescript-react/Dockerfile
      DOCKER_TAGS: ghcr.io/${{ github.repository_owner }}/autotests-ai-multistack-app-frontend-typescript-react:${{ github.sha }}
      DOCKER_BUILD_ARGS: |
        REACT_UI=frontend/_shared/frontend-react-ui
        UI_RUNTIME=frontend/_shared/frontend-javascript-app
      DOCKER_OCI_LABELS: |
        org.opencontainers.image.title=autotests-ai-multistack-app-frontend-typescript-react
        org.opencontainers.image.description=autotests-ai-multistack-app · frontend-typescript-react
        org.opencontainers.image.source=https://github.com/autotests-ai/autotests-ai-multistack-app
        org.opencontainers.image.url=https://autotests.ai/stack/backend-java-spring/frontend-typescript-react/
        org.opencontainers.image.vendor=autotests-ai
    steps:
      - *checkout
      - *docker-login-ghcr
      - *setup-docker-buildx
      - *docker-build-push
      - name: Push frontend Docker image to GHCR
        run: docker compose push ${{ env.FRONTEND }}

  deploy-backend:
    needs: [build-backend, sonar-backend, trigger, e2e-tests-stage]
    if: >-
      !cancelled() &&
      needs.build-backend.result == 'success' &&
      needs.sonar-backend.result == 'success' &&
      needs.trigger.outputs.cd_production == 'true' &&
      needs.e2e-tests-stage.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment:
      name: ${{ vars.DEPLOY_ENVIRONMENT || 'multistack-production' }}
      url: ${{ env.APP_URL }}
    # Shared deploy host: serialize git checkout with deploy-frontend (same checkout dir).
    concurrency:
      group: deploy-autotests-ai-multistack-app
      cancel-in-progress: false
    # Backend (+ postgres) — api-tests need API :8800 after deploy, not frontend.
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_STEP_NAME: Deploy backend to stand
      DEPLOY_APP_DIR: /home/autotests_ai_multistack/autotests-ai-multistack-app
      DEPLOY_COMPOSE_PROJECT: autotests-ai-multistack-app
      DEPLOY_COMPOSE_ENV_FILE: ""
      DEPLOY_COMPOSE_SERVICES: postgres postgres-ensure-dbs backend-java-spring
      DEPLOY_HEALTH_URL: http://127.0.0.1:8800/api/health
    steps:
      - &ssh-deploy
        name: ${{ env.DEPLOY_STEP_NAME }}
        uses: appleboy/ssh-action@v1.2.5
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ vars.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          command_timeout: 15m
          envs: IMAGE_TAG,GHCR_TOKEN,DEPLOY_COMPOSE_SERVICES,DEPLOY_HEALTH_URL,DEPLOY_APP_DIR,DEPLOY_COMPOSE_PROJECT,DEPLOY_COMPOSE_ENV_FILE
          script: ${{ env.DEPLOY_SSH_SCRIPT }}

  # FE deploy after teaching build + sonar. One FRONTEND — no sibling matrix.
  deploy-frontend:
    needs: [build-frontend, sonar-frontend, trigger, e2e-tests-stage]
    if: >-
      !cancelled() &&
      needs.build-frontend.result == 'success' &&
      needs.sonar-frontend.result == 'success' &&
      needs.trigger.outputs.cd_production == 'true' &&
      needs.e2e-tests-stage.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment:
      name: ${{ vars.DEPLOY_ENVIRONMENT || 'multistack-production' }}
      url: ${{ env.APP_URL }}
    # Independent of backend lane; shared host lock via concurrency (same checkout dir).
    concurrency:
      group: deploy-autotests-ai-multistack-app
      cancel-in-progress: false
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_STEP_NAME: Deploy frontend to stand
      DEPLOY_APP_DIR: /home/autotests_ai_multistack/autotests-ai-multistack-app
      DEPLOY_COMPOSE_PROJECT: autotests-ai-multistack-app
      DEPLOY_COMPOSE_ENV_FILE: ""
      DEPLOY_COMPOSE_SERVICES: frontend-typescript-react
      DEPLOY_HEALTH_URL: ""
    steps:
      - *ssh-deploy

  # Stage twin — push develop (WIP) or push main / dispatch production|both (promotion).
  # Own checkout. Skip until repo var STAGE_APP_DIR is set.
  deploy-backend-stage:
    needs: [build-backend, sonar-backend, trigger, preempt-develop-cd, yield-to-main-cd]
    if: >-
      always() && !cancelled() &&
      needs.build-backend.result == 'success' &&
      needs.sonar-backend.result == 'success' &&
      vars.STAGE_APP_DIR != '' &&
      needs.trigger.outputs.cd_stage == 'true' &&
      (needs.preempt-develop-cd.result == 'success' || needs.preempt-develop-cd.result == 'skipped') &&
      (needs.yield-to-main-cd.result == 'success' || needs.yield-to-main-cd.result == 'skipped')
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment:
      name: multistack-stage
      url: ${{ env.STAGE_APP_URL }}
    concurrency:
      group: deploy-autotests-ai-multistack-app-stage
      cancel-in-progress: false
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_STEP_NAME: Deploy backend to stage
      DEPLOY_APP_DIR: ${{ vars.STAGE_APP_DIR }}
      DEPLOY_COMPOSE_PROJECT: autotests-ai-multistack-app-stage
      DEPLOY_COMPOSE_ENV_FILE: deploy/stage.env
      DEPLOY_COMPOSE_SERVICES: postgres postgres-ensure-dbs backend-java-spring
      DEPLOY_HEALTH_URL: http://127.0.0.1:18800/api/health
    steps:
      - *ssh-deploy

  deploy-frontend-stage:
    needs: [build-frontend, sonar-frontend, trigger, preempt-develop-cd, yield-to-main-cd]
    if: >-
      always() && !cancelled() &&
      needs.build-frontend.result == 'success' &&
      needs.sonar-frontend.result == 'success' &&
      vars.STAGE_APP_DIR != '' &&
      needs.trigger.outputs.cd_stage == 'true' &&
      (needs.preempt-develop-cd.result == 'success' || needs.preempt-develop-cd.result == 'skipped') &&
      (needs.yield-to-main-cd.result == 'success' || needs.yield-to-main-cd.result == 'skipped')
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment:
      name: multistack-stage
      url: ${{ env.STAGE_APP_URL }}
    concurrency:
      group: deploy-autotests-ai-multistack-app-stage
      cancel-in-progress: false
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_STEP_NAME: Deploy frontend to stage
      DEPLOY_APP_DIR: ${{ vars.STAGE_APP_DIR }}
      DEPLOY_COMPOSE_PROJECT: autotests-ai-multistack-app-stage
      DEPLOY_COMPOSE_ENV_FILE: deploy/stage.env
      DEPLOY_COMPOSE_SERVICES: frontend-typescript-react
      DEPLOY_HEALTH_URL: ""
    steps:
      - *ssh-deploy

  # Rest Assured against deployed backend — starts as soon as that lane finishes (FE may
  # still be deploying). Dispatch can run api without a backend deploy this run.
  api-tests:
    needs: [deploy-backend, trigger]
    if: >-
      always() && !cancelled() &&
      needs.trigger.outputs.cd_production == 'true' && (
        (needs.trigger.outputs.backend == 'true' &&
         needs.deploy-backend.result == 'success') ||
        (needs.trigger.outputs.tests == 'true' &&
         (needs.deploy-backend.result == 'success' ||
          needs.deploy-backend.result == 'skipped')) ||
        (github.event_name == 'workflow_dispatch' && inputs.run_api &&
         (needs.deploy-backend.result == 'success' ||
          needs.deploy-backend.result == 'skipped') &&
         (needs.trigger.outputs.backend != 'true' ||
          needs.deploy-backend.result == 'success'))
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      LAYER: api
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      # Share tests-module GUH with tests-harness (setup-gradle@v6 keys on github.job).
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "false"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      PLAYWRIGHT_IF: "false"
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/requirements.txt
      PYTHON_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      ALLURE_RESULTS_ARTIFACT: allure-results-api
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - name: Wait for live API
        env:
          API_HEALTH: https://${{ env.PUBLIC_HOST }}/${{ env.STACK_MOUNT }}/${{ env.BACKEND }}/api/health
        run: |
          set -euo pipefail
          echo "==> API ${API_HEALTH}"
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${API_HEALTH}"
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      # ── java (no UI — Rest Assured API contract against deployed backend) ─
      - *setup-java
      - *setup-gradle
      - name: Run API tests (Gradle)
        if: env.TESTS_LANG == 'java'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -Denv=prod "-DincludeTags=api&prod"
        working-directory: ${{ env.TESTS_DIR }}
      # ── javascript / typescript (full suite) ──────────────────────────────
      - *setup-node
      - *npm-ci
      - *cache-playwright-browsers
      - *install-playwright
      - name: Run tests (npm)
        if: env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test
        working-directory: ${{ env.TESTS_DIR }}

      # ── python (full suite) ──────────────────────────────────────────────
      - *setup-python
      - *pip-install
      - name: Run Python tests (pytest)
        if: env.TESTS_LANG == 'python'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl python -m pytest
        working-directory: ${{ env.TESTS_DIR }}
      - *upload-allure-results

  # After API layer; also wait for frontend deploy (skipped = keep live UI).
  # Frontend-only push skips api-tests and still runs e2e.
  e2e-tests:
    needs: [api-tests, deploy-frontend, trigger]
    if: >-
      always() && !cancelled() &&
      needs.trigger.outputs.cd_production == 'true' && (
        (needs.deploy-frontend.result == 'success' ||
         needs.deploy-frontend.result == 'skipped') && (
          (github.event_name == 'push' && github.ref == 'refs/heads/main' &&
           (needs.api-tests.result == 'success' ||
            (needs.api-tests.result == 'skipped' &&
             needs.deploy-frontend.result == 'success'))) ||
          (needs.trigger.outputs.tests == 'true' &&
           github.event_name == 'workflow_dispatch' &&
           (needs.api-tests.result == 'success' ||
            needs.api-tests.result == 'skipped')) ||
          (github.event_name == 'workflow_dispatch' &&
           (inputs.run_e2e ||
            inputs.run_screenshot ||
            inputs.update_e2e_screenshots ||
            inputs.include_tags != '' ||
            inputs.exclude_tags != '') &&
           (needs.api-tests.result == 'success' || needs.api-tests.result == 'skipped') &&
           (needs.trigger.outputs.frontend != 'true' ||
            needs.deploy-frontend.result == 'success'))
        )
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      LAYER: e2e
      SCREENSHOT_OS: linux
      SCREENSHOT_BROWSER: chrome
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      # Share tests-module GUH with tests-harness (setup-gradle@v6 keys on github.job).
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "false"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      PLAYWRIGHT_IF: "false"
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/requirements.txt
      PYTHON_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      ALLURE_RESULTS_ARTIFACT: allure-results-e2e
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - name: Wait for live UI
        run: |
          set -euo pipefail
          echo "==> UI ${UI_URL}"
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${UI_URL}"
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      # ── java: -DincludeTags / -DexcludeTags (override via dispatch inputs) ─
      - *setup-java
      - *setup-gradle
      - name: Run e2e tests (Gradle with tag filters)
        if: >-
          env.TESTS_LANG == 'java' && (
            github.event_name == 'push' ||
            inputs.run_e2e ||
            inputs.include_tags != '' ||
            inputs.exclude_tags != ''
          )
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: prod
          INCLUDE_TAGS: ${{ inputs.include_tags }}
          EXCLUDE_TAGS: ${{ inputs.exclude_tags }}
          # Selenoid hub URL with credentials — config files commit only the creds-less URL.
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          set -euo pipefail
          ARGS=(-Denv="$RUN_ENV")
          if [ -n "${SELENOID_REMOTE_URL:-}" ]; then
            ARGS+=(-DremoteUrl="${SELENOID_REMOTE_URL}")
          fi
          if [ -n "${INCLUDE_TAGS}" ]; then
            ARGS+=(-DincludeTags="${INCLUDE_TAGS}")
          else
            ARGS+=("-DincludeTags=e2e&prod")
          fi
          if [ -n "${EXCLUDE_TAGS}" ]; then
            ARGS+=(-DexcludeTags="${EXCLUDE_TAGS}")
          else
            ARGS+=(-DexcludeTags=mock,screenshot)
          fi
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "${ARGS[@]}"
        working-directory: ${{ env.TESTS_DIR }}
      - name: Stash e2e Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-e2e-stash"
          SRC="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$STASH"
          if [ -d "$SRC" ]; then cp -R "$SRC/." "$STASH/"; fi
      - name: Run screenshot e2e compare (Gradle)
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' &&
          inputs.run_screenshot &&
          inputs.update_e2e_screenshots != true
        id: run-screenshot-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: prod
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "-Denv=$RUN_ENV" -DincludeTags=screenshot -DexcludeTags=mock ${SELENOID_REMOTE_URL:+"-DremoteUrl=$SELENOID_REMOTE_URL"}
        working-directory: ${{ env.TESTS_DIR }}
      - name: Update screenshots
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_e2e_screenshots
        id: update-screenshots
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: prod
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "-Denv=$RUN_ENV" -DincludeTags=screenshot -DupdateScreenshots=true ${SELENOID_REMOTE_URL:+"-DremoteUrl=$SELENOID_REMOTE_URL"}
        working-directory: ${{ env.TESTS_DIR }}
      - name: Merge Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-e2e-stash"
          DEST="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$DEST"
          if [ -d "$STASH" ]; then cp -R "$STASH/." "$DEST/"; fi
      - name: Publish e2e screenshot PNGs
        if: >-
          always() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_e2e_screenshots
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: e2e-screenshot-pngs
          path: ${{ env.TESTS_DIR }}/src/test/resources/screenshots/
          if-no-files-found: ignore
      # ── javascript / typescript (full suite; no tag pyramid yet) ──────────
      - *setup-node
      - *npm-ci
      - *cache-playwright-browsers
      - *install-playwright
      - name: Run tests (npm)
        if: env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test
        working-directory: ${{ env.TESTS_DIR }}

      # ── python (full suite; no tag pyramid yet) ───────────────────────────
      - *setup-python
      - *pip-install
      - name: Run Python tests (pytest)
        if: env.TESTS_LANG == 'python'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl python -m pytest
        working-directory: ${{ env.TESTS_DIR }}
      - *upload-allure-results

  # Stage twin of api-tests — full api layer vs STAGE_APP_URL (push develop / deploy_target=stage).
  api-tests-stage:
    needs: [deploy-backend-stage, trigger]
    if: >-
      always() && !cancelled() &&
      needs.trigger.outputs.cd_stage == 'true' && (
        (needs.trigger.outputs.backend == 'true' &&
         needs.deploy-backend-stage.result == 'success') ||
        (needs.trigger.outputs.tests == 'true' &&
         (needs.deploy-backend-stage.result == 'success' ||
          needs.deploy-backend-stage.result == 'skipped')) ||
        (github.event_name == 'workflow_dispatch' && inputs.run_api &&
         (needs.deploy-backend-stage.result == 'success' ||
          needs.deploy-backend-stage.result == 'skipped') &&
         (needs.trigger.outputs.backend != 'true' ||
          needs.deploy-backend-stage.result == 'success'))
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      LAYER: api
      ENDPOINT: stage
      PUBLIC_HOST: stage.autotests.ai
      APP_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      UI_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      BASE_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "false"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      PLAYWRIGHT_IF: "false"
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/requirements.txt
      PYTHON_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      ALLURE_RESULTS_ARTIFACT: allure-results-api-stage
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - name: Wait for live API
        env:
          API_HEALTH: https://stage.autotests.ai/stack/backend-java-spring/api/health
        run: |
          set -euo pipefail
          echo "==> API ${API_HEALTH}"
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${API_HEALTH}"
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      - *setup-java
      - *setup-gradle
      - name: Run API tests (Gradle)
        if: env.TESTS_LANG == 'java'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test -Denv=stage -DincludeTags=api
        working-directory: ${{ env.TESTS_DIR }}
      - *setup-node
      - *npm-ci
      - *cache-playwright-browsers
      - *install-playwright
      - name: Run tests (npm)
        if: env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test
        working-directory: ${{ env.TESTS_DIR }}
      - *setup-python
      - *pip-install
      - name: Run Python tests (pytest)
        if: env.TESTS_LANG == 'python'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl python -m pytest
        working-directory: ${{ env.TESTS_DIR }}
      - *upload-allure-results

  # Stage twin of e2e-tests — full e2e vs STAGE_APP_URL, then screenshot compare.
  e2e-tests-stage:
    needs: [api-tests-stage, deploy-frontend-stage, trigger]
    if: >-
      always() && !cancelled() &&
      needs.trigger.outputs.cd_stage == 'true' && (
        (needs.deploy-frontend-stage.result == 'success' ||
         needs.deploy-frontend-stage.result == 'skipped') && (
          (github.event_name == 'push' &&
           (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main') &&
           (needs.api-tests-stage.result == 'success' ||
            (needs.api-tests-stage.result == 'skipped' &&
             needs.deploy-frontend-stage.result == 'success'))) ||
          (needs.trigger.outputs.tests == 'true' &&
           github.event_name == 'workflow_dispatch' &&
           (needs.api-tests-stage.result == 'success' ||
            needs.api-tests-stage.result == 'skipped')) ||
          (github.event_name == 'workflow_dispatch' &&
           (inputs.run_e2e ||
            inputs.run_screenshot ||
            inputs.update_stage_screenshots ||
            inputs.include_tags != '' ||
            inputs.exclude_tags != '') &&
           (needs.api-tests-stage.result == 'success' || needs.api-tests-stage.result == 'skipped') &&
           (needs.trigger.outputs.frontend != 'true' ||
            needs.deploy-frontend-stage.result == 'success'))
        )
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      LAYER: e2e
      ENDPOINT: stage
      PUBLIC_HOST: stage.autotests.ai
      APP_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      UI_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      BASE_URL: https://stage.autotests.ai/stack/backend-java-spring/frontend-typescript-react/
      SCREENSHOT_OS: linux
      SCREENSHOT_BROWSER: chrome
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      SETUP_NODE_IF: "false"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "false"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      PLAYWRIGHT_IF: "false"
      SETUP_PYTHON_IF: "false"
      PYTHON_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/requirements.txt
      PYTHON_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
      ALLURE_RESULTS_ARTIFACT: allure-results-e2e-stage
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - name: Wait for live UI
        run: |
          set -euo pipefail
          echo "==> UI ${UI_URL}"
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${UI_URL}"
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      - *setup-java
      - *setup-gradle
      - name: Run e2e tests (Gradle with tag filters)
        if: >-
          env.TESTS_LANG == 'java' && (
            github.event_name == 'push' ||
            inputs.run_e2e ||
            inputs.include_tags != '' ||
            inputs.exclude_tags != ''
          )
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: stage
          INCLUDE_TAGS: ${{ inputs.include_tags }}
          EXCLUDE_TAGS: ${{ inputs.exclude_tags }}
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          set -euo pipefail
          ARGS=(-Denv="$RUN_ENV")
          if [ -n "${SELENOID_REMOTE_URL:-}" ]; then
            ARGS+=(-DremoteUrl="${SELENOID_REMOTE_URL}")
          fi
          if [ -n "${INCLUDE_TAGS}" ]; then
            ARGS+=(-DincludeTags="${INCLUDE_TAGS}")
          else
            ARGS+=(-DincludeTags=e2e)
          fi
          if [ -n "${EXCLUDE_TAGS}" ]; then
            ARGS+=(-DexcludeTags="${EXCLUDE_TAGS}")
          else
            ARGS+=(-DexcludeTags=mock,screenshot)
          fi
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "${ARGS[@]}"
        working-directory: ${{ env.TESTS_DIR }}
      - name: Stash e2e Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-e2e-stage-stash"
          SRC="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$STASH"
          if [ -d "$SRC" ]; then cp -R "$SRC/." "$STASH/"; fi
      - name: Run screenshot stage compare (Gradle)
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          (github.event_name != 'workflow_dispatch' || inputs.update_stage_screenshots != true)
        id: run-screenshot-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: stage
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "-Denv=$RUN_ENV" -DincludeTags=screenshot -DexcludeTags=mock ${SELENOID_REMOTE_URL:+"-DremoteUrl=$SELENOID_REMOTE_URL"}
        working-directory: ${{ env.TESTS_DIR }}
      - name: Update screenshots
        if: >-
          always() && !cancelled() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_stage_screenshots
        id: update-screenshots
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: stage
          SELENOID_REMOTE_URL: ${{ secrets.SELENOID_REMOTE_URL }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "-Denv=$RUN_ENV" -DincludeTags=screenshot -DupdateScreenshots=true ${SELENOID_REMOTE_URL:+"-DremoteUrl=$SELENOID_REMOTE_URL"}
        working-directory: ${{ env.TESTS_DIR }}
      - name: Merge Allure results
        if: always() && env.TESTS_LANG == 'java'
        continue-on-error: true
        run: |
          set -euo pipefail
          STASH="${{ env.TESTS_DIR }}/build/allure-results-e2e-stage-stash"
          DEST="${{ env.TESTS_DIR }}/build/allure-results"
          mkdir -p "$DEST"
          if [ -d "$STASH" ]; then cp -R "$STASH/." "$DEST/"; fi
      - name: Publish stage screenshot PNGs
        if: >-
          always() && env.TESTS_LANG == 'java' &&
          github.event_name == 'workflow_dispatch' && inputs.update_stage_screenshots
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: stage-screenshot-pngs
          path: ${{ env.TESTS_DIR }}/src/test/resources/screenshots/
          if-no-files-found: ignore
      - *setup-node
      - *npm-ci
      - *cache-playwright-browsers
      - *install-playwright
      - name: Run tests (npm)
        if: env.TESTS_LANG == 'javascript' || env.TESTS_LANG == 'typescript'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl npm test
        working-directory: ${{ env.TESTS_DIR }}
      - *setup-python
      - *pip-install
      - name: Run Python tests (pytest)
        if: env.TESTS_LANG == 'python'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl python -m pytest
        working-directory: ${{ env.TESTS_DIR }}
      - *upload-allure-results

  # Main-chain tip after e2e; manual execution is explicit.
  manual-tests:
    needs: [e2e-tests, trigger]
    if: >-
      always() && !cancelled() &&
      github.event_name == 'workflow_dispatch' &&
      inputs.run_manual &&
      (needs.e2e-tests.result == 'success' || needs.e2e-tests.result == 'skipped')
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      LAYER: manual
      ALLURE_LIVE_ENABLED: ${{ needs.trigger.outputs.enabled }}
      ALLURE_LAUNCH_ID: ${{ needs.trigger.outputs.launch_id }}
      ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
      SETUP_JVM_IF: "true"
      GRADLE_CACHE_READ_ONLY: "true"
      # Share tests-module GUH with tests-harness (setup-gradle@v6 keys on github.job).
      GRADLE_BUILD_ACTION_CACHE_KEY_JOB: tests-harness
      ALLURE_RESULTS_ARTIFACT: allure-results-manual
      ALLURE_RESULTS_UPLOAD_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/build/allure-results/**
    steps:
      - name: Wait for live UI
        run: |
          set -euo pipefail
          echo "==> UI ${UI_URL}"
          curl -fsS --retry 30 --retry-delay 2 --retry-all-errors "${UI_URL}"
      - *checkout
      - *cache-allurectl
      - *setup-live-allurectl

      # ── java (-DincludeTags=manual) ───────────────────────────────────────
      - *setup-java
      - *setup-gradle
      - name: Run manual tests (Gradle)
        if: env.TESTS_LANG == 'java'
        id: run-tests
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          RUN_ENV: prod
        run: |
          eval "${ALLURECTL_RUN}"
          run_with_allurectl ./gradlew test "-Denv=$RUN_ENV" -DincludeTags=manual
        working-directory: ${{ env.TESTS_DIR }}
      - *upload-allure-results

  # Gating npm ci contract: exact pins in package.json must match package-lock.json.
  # Independent of the pyramid so lock drift fails the workflow without waiting
  # for publish-allure-report.
  # Gating Allure generate after e2e. Lock pins + npm ci live here (not extra
  # graph boxes). Pages push + Telegram are soft after generate; live Pages
  # deploy is the following job (deploy-pages@v5).
  publish-allure-report:
    name: publish-allure-report
    needs:
      - unit-tests
      - component-tests
      - ui-mock-tests
      - integration-tests
      - api-tests
      - e2e-tests
      - api-tests-stage
      - e2e-tests-stage
      - manual-tests
      - trigger
      - sonar-backend
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: write
      actions: read
      pages: write
    env:
      REPORT_ID: ${{ github.run_id }}
      GH_PAGES_BASE: ${{ vars.GH_PAGES_BASE || format('https://{0}.github.io/{1}', github.repository_owner, github.event.repository.name) }}
      ALLURE_LIVE_ENABLED: ${{ vars.ALLURE_PROJECT_ID != '' }}
      SETUP_NODE_IF: "true"
      NODE_CACHE_DEPENDENCY_PATH: tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json
      NPM_CI_IF: "true"
      NPM_WORKDIR: tests/java/tests-java-gradle-junit5-allure3-selenide
    outputs:
      testops_launch_url: ${{ steps.resolve-testops-url.outputs.launch_url }}
      pages_artifact: ${{ steps.pages-artifact.outcome }}
    steps:
      - *checkout

      - *setup-node

      - name: package.json pins must match package-lock.json
        working-directory: ${{ env.TESTS_DIR }}
        run: node scripts/check-package-lock.mjs

      - name: Load Allure history
        continue-on-error: true
        uses: actions/checkout@v7
        with:
          ref: gh-pages
          path: gh-pages
          sparse-checkout: history.jsonl
          sparse-checkout-cone-mode: false

      - &cache-allure-nm
        name: Cache Allure npm node_modules
        id: cache-allure-nm
        continue-on-error: true
        uses: actions/cache@v6
        with:
          path: tests/java/tests-java-gradle-junit5-allure3-selenide/node_modules
          key: allure-nm-${{ runner.os }}-${{ hashFiles('tests/java/tests-java-gradle-junit5-allure3-selenide/package-lock.json') }}

      - name: Skip npm ci on node_modules cache hit
        if: steps.cache-allure-nm.outcome == 'success' && steps.cache-allure-nm.outputs.cache-hit == 'true'
        run: echo "NPM_CI_IF=false" >> "$GITHUB_ENV"

      - *npm-ci

      - name: Download merged raw Allure results
        id: results
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: allure-results-*
          path: ${{ env.ALLURE_RESULTS_DIR }}
          merge-multiple: true

      - name: Verify merged Allure results
        id: merged-results
        if: steps.results.outcome == 'success'
        env:
          ALLURE_RESULTS: ${{ env.ALLURE_RESULTS_DIR }}
        run: |
          if [ -n "$(find "${ALLURE_RESULTS}" -type f 2>/dev/null | head -1)" ]; then
            echo "has_results=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_results=false" >> "$GITHUB_OUTPUT"
            echo "No merged Allure results — skip publish"
          fi

      # Canonical environment.properties after merge (one file for Pages / fallback upload).
      - name: Write launch environment axes
        if: steps.merged-results.outputs.has_results == 'true'
        env:
          ALLURE_RESULTS: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
        run: |
          eval "${ALLURE_WRITE_ENVIRONMENT}"
          write_allure_environment

      - name: Restore Allure history
        if: steps.merged-results.outputs.has_results == 'true'
        run: |
          if [ -f gh-pages/history.jsonl ]; then
            cp gh-pages/history.jsonl "${{ env.TESTS_DIR }}/history.jsonl"
          elif [ -f "${{ env.TESTS_DIR }}/history.jsonl" ]; then
            echo "Using checked-in history.jsonl"
          fi

      - *cache-allurectl
      - *setup-live-allurectl

      - name: Write executor.json
        if: steps.merged-results.outputs.has_results == 'true'
        env:
          ALLURE_RESULTS: ${{ env.ALLURE_RESULTS_DIR }}
        run: |
          mkdir -p "${ALLURE_RESULTS}"
          cat > "${ALLURE_RESULTS}/executor.json" <<EOF
          {
            "reportName": "Run #${{ github.run_number }}",
            "buildOrder": ${{ github.run_number }},
            "reportUrl": "${GH_PAGES_BASE}/reports/${REPORT_ID}/awesome/index.html",
            "buildUrl": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }
          EOF

      - name: Fallback upload merged results to Allure TestOps
        if: >-
          always() &&
          needs.trigger.outputs.enabled != 'true' &&
          steps.merged-results.outputs.has_results == 'true' &&
          env.ALLURE_PROJECT_ID != ''
        continue-on-error: true
        id: testops-upload
        working-directory: ${{ env.TESTS_DIR }}
        env:
          ALLURE_TOKEN: ${{ secrets.ALLURE_TOKEN }}
          ALLURE_LAUNCH_NAME: "${{ env.APP_SLUG }} CI — ${{ env.RUN_LABEL }}"
        run: |
          set +e
          set -uo pipefail
          if [ -z "${ALLURE_TOKEN:-}" ]; then
            echo "No ALLURE_TOKEN — skip TestOps upload"
            exit 0
          fi
          if ! command -v allurectl >/dev/null 2>&1; then
            echo "allurectl not available — skip TestOps upload"
            exit 0
          fi
          allurectl upload "build/allure-results" \
            --endpoint "${ALLURE_ENDPOINT}" \
            --token "${ALLURE_TOKEN}" \
            --project-id "${ALLURE_PROJECT_ID}" \
            --launch-name "${ALLURE_LAUNCH_NAME}" 2>&1 | tee allurectl-upload.log
          UPLOAD_EXIT=${PIPESTATUS[0]}
          if [ "$UPLOAD_EXIT" -eq 0 ]; then
            LAUNCH_URL=$(grep -Eo 'https://[^ ]+/launch/[0-9]+' allurectl-upload.log | head -1 || true)
            if [ -z "$LAUNCH_URL" ] && allurectl job-run env >/tmp/allure-env 2>/dev/null; then
              # shellcheck disable=SC1091
              source /tmp/allure-env
              LAUNCH_URL="${ALLURE_LAUNCH_URL:-}"
            fi
            if [ -n "$LAUNCH_URL" ]; then
              echo "launch_url=${LAUNCH_URL}" >> "$GITHUB_OUTPUT"
              echo "TESTOPS_LAUNCH_URL=${LAUNCH_URL}" >> "$GITHUB_ENV"
            fi
          fi
          exit 0

      - name: Stop shared TestOps job-run
        if: >-
          always() &&
          needs.trigger.outputs.job_run_id != '' &&
          steps.setup-allurectl.outcome == 'success'
        continue-on-error: true
        env:
          ALLURE_JOB_RUN_ID: ${{ needs.trigger.outputs.job_run_id }}
        run: |
          set -euo pipefail
          allurectl --http-timeout 30s job-run stop \
            --project-id "$ALLURE_PROJECT_ID" \
            --job-run-id "$ALLURE_JOB_RUN_ID"

      - name: Resolve TestOps launch URL
        id: resolve-testops-url
        if: always()
        env:
          LIVE_LAUNCH_URL: ${{ needs.trigger.outputs.launch_url }}
          FALLBACK_LAUNCH_URL: ${{ steps.testops-upload.outputs.launch_url }}
        run: |
          LAUNCH_URL="${LIVE_LAUNCH_URL:-${FALLBACK_LAUNCH_URL:-}}"
          echo "launch_url=${LAUNCH_URL}" >> "$GITHUB_OUTPUT"
          if [ -n "$LAUNCH_URL" ]; then
            echo "TESTOPS_LAUNCH_URL=${LAUNCH_URL}" >> "$GITHUB_ENV"
          fi

      - name: Download Sonar backend report-task
        id: sonar-report-task
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          name: sonar-backend-report-task
          path: sonar-backend-report-task

      - name: Attach live Sonar quality gate
        if: steps.merged-results.outputs.has_results == 'true'
        continue-on-error: true
        working-directory: ${{ env.TESTS_DIR }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ env.SONAR_HOST_URL }}
        run: |
          set -euo pipefail
          REPORT_TASK="$(find "${GITHUB_WORKSPACE}/sonar-backend-report-task" -name 'report-task.txt' -print -quit 2>/dev/null || true)"
          if [[ -z "${REPORT_TASK}" || ! -f "${REPORT_TASK}" ]]; then
            echo "No Sonar report-task — Allure panel keeps fixture"
            exit 0
          fi
          if [[ -z "${SONAR_TOKEN:-}" ]]; then
            echo "SONAR_TOKEN unset — Allure panel keeps fixture"
            exit 0
          fi
          node scripts/dump-sonar-quality-gate.mjs \
            --report-task "${REPORT_TASK}" \
            --out allure/sonar-quality-gate.json \
            --timeout "${SONAR_QUALITY_GATE_TIMEOUT}"

      - name: Allure quality gate + report
        if: always() && steps.merged-results.outputs.has_results == 'true'
        id: allure-report
        working-directory: ${{ env.TESTS_DIR }}
        run: |
          set +e
          npx --no-install allure quality-gate build/allure-results --config allurerc.mjs
          echo "QUALITY_GATE_EXIT=$?" >> "$GITHUB_ENV"
          npx --no-install allure generate build/allure-results \
            -o build/reports/allure-report/allureReport \
            --config allurerc.mjs
          REPORT_EXIT=$?
          if [ "${REPORT_EXIT}" -eq 0 ]; then
            node allure/tests-table-from-run.mjs \
              --results build/allure-results \
              --history history.jsonl \
              --report build/reports/allure-report/allureReport
          fi
          set -e
          exit "${REPORT_EXIT}"

      - name: Publish merged Allure report artifact
        if: always() && steps.allure-report.outcome == 'success'
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: allure-report-ci
          path: ${{ env.ALLURE_REPORT_DIR }}/
          if-no-files-found: ignore
          retention-days: 14

      # Soft Pages — same job so generate stays the gate; PR skips these steps.
      - name: Prepare GitHub Pages
        id: pages-prepare
        if: >-
          always() &&
          github.event_name != 'pull_request' &&
          steps.allure-report.outcome == 'success'
        continue-on-error: true
        env:
          ALLURE_REPORT_ROOT: ${{ env.ALLURE_REPORT_DIR }}
          ALLURE_AWESOME: ${{ env.ALLURE_AWESOME_DIR }}
          ALLURE_RESULTS: ${{ env.ALLURE_RESULTS_DIR }}
        run: |
          if [ ! -f "${ALLURE_AWESOME}/index.html" ]; then
            echo "No ${ALLURE_AWESOME}/index.html — skip GitHub Pages"
            exit 0
          fi
          mkdir -p pages/reports/${REPORT_ID}
          cp -r "${ALLURE_REPORT_ROOT}/." "pages/reports/${REPORT_ID}/"
          rm -rf pages/reports/latest
          cp -r "pages/reports/${REPORT_ID}" pages/reports/latest
          if [ -f "${{ env.TESTS_DIR }}/history.jsonl" ]; then
            cp "${{ env.TESTS_DIR }}/history.jsonl" pages/history.jsonl
          elif [ -f "${ALLURE_REPORT_ROOT}/history.jsonl" ]; then
            cp "${ALLURE_REPORT_ROOT}/history.jsonl" pages/history.jsonl
          fi
          echo "${REPORT_ID}" > pages/latest-run-id.txt
          printf 'reports.autotests.ai\n' > pages/CNAME
          touch pages/.nojekyll
          printf '%s\n' '<!DOCTYPE html><meta charset="utf-8"><meta http-equiv="refresh" content="0;url=reports/latest/awesome/index.html"><link rel="canonical" href="reports/latest/awesome/index.html"><title>Allure</title>' > pages/index.html
          mkdir -p pages/readme
          bash .github/scripts/generate-readme-badge.sh \
            "${ALLURE_RESULTS}" pages/readme main "#${{ github.run_number }}" \
            "" "Multistack" "Java 21 · Spring Boot · Allure 3" ""

      - name: Upload GitHub Pages artifact
        id: pages-artifact
        if: >-
          always() &&
          github.event_name != 'pull_request' &&
          steps.allure-report.outcome == 'success' &&
          steps.pages-prepare.outcome == 'success' &&
          hashFiles('pages/reports/**/awesome/index.html') != ''
        continue-on-error: true
        uses: actions/upload-pages-artifact@v5
        with:
          path: pages

      - name: Backup Allure report on gh-pages
        id: gh-pages-deploy
        if: >-
          always() &&
          github.event_name != 'pull_request' &&
          steps.allure-report.outcome == 'success' &&
          steps.pages-prepare.outcome == 'success' &&
          hashFiles('pages/reports/**/awesome/index.html') != ''
        continue-on-error: true
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: pages
          enable_jekyll: false
          # Branch backup only (history.jsonl + one run). Live site is
          # deploy-github-pages / pages.yml. keep_files: true bloated the
          # branch (~12GB) when it was also the Pages source.
          keep_files: false
          cname: reports.autotests.ai

      - name: Send Allure Telegram notification
        if: always() && steps.allure-report.outcome == 'success'
        continue-on-error: true
        timeout-minutes: 2
        working-directory: ${{ env.TESTS_DIR }}
        env:
          NOTIFICATION_PROJECT: "${{ env.APP_SLUG }} CI #${{ github.run_number }}"
          BUILD_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TESTOPS_LAUNCH_URL: ${{ steps.resolve-testops-url.outputs.launch_url }}
          ALLURE_FOLDER: ${{ github.workspace }}/${{ env.ALLURE_AWESOME_DIR }}
          ALLURE_RESULTS_FOLDER: ${{ github.workspace }}/${{ env.ALLURE_RESULTS_DIR }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ vars.TELEGRAM_CHAT_ID || '-1004381150566' }}
          TELEGRAM_TOPIC_ID: ${{ vars.TELEGRAM_TOPIC_ID || secrets.TELEGRAM_TOPIC_ID || '3' }}
          NOTIFICATION_MODE: ${{ github.event_name != 'pull_request' && 'live' || 'dry-run' }}
          PAGES_OUTCOME: ${{ steps.gh-pages-deploy.outcome }}
        run: |
          set -euo pipefail
          if [ -z "${TELEGRAM_BOT_TOKEN:-}" ]; then
            echo "No Telegram token — skip notification"
            exit 0
          fi
          if [ ! -f "${ALLURE_FOLDER}/summary.json" ]; then
            echo "No ${ALLURE_FOLDER}/summary.json — skip Telegram"
            exit 0
          fi
          if [ "${PAGES_OUTCOME}" = "success" ]; then
            export ALLURE_REPORT_URL="${GH_PAGES_BASE}/reports/${REPORT_ID}/awesome/index.html"
            export ALLURE_DASHBOARD_URL="${GH_PAGES_BASE}/reports/${REPORT_ID}/dashboard/index.html"
          else
            export ALLURE_REPORT_URL=""
            export ALLURE_DASHBOARD_URL=""
          fi
          node scripts/render-notifications-config.mjs
          if [ "${NOTIFICATION_MODE}" = "live" ]; then
            MODE_FLAG="--live"
          else
            MODE_FLAG="--dry-run"
          fi
          npx --no-install allure-notifications send \
            --config notifications/config.runtime.json \
            "${MODE_FLAG}" \
            --out collage.png

      - name: Job summary
        if: always()
        env:
          ALLURE_AWESOME: ${{ env.ALLURE_AWESOME_DIR }}
          PAGES_OUTCOME: ${{ steps.gh-pages-deploy.outcome }}
        run: |
          {
            echo "## publish-allure-report"
            echo ""
            echo "- [GitHub Actions run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            if [ -f "${ALLURE_AWESOME}/index.html" ]; then
              echo "- Allure report artifact: allure-report-ci"
            fi
            if [ "${PAGES_OUTCOME}" = "success" ]; then
              echo "- [Allure report](${GH_PAGES_BASE}/reports/latest/awesome/index.html)"
              echo "- [Allure dashboard](${GH_PAGES_BASE}/reports/latest/dashboard/index.html)"
            elif [ "${{ github.event_name }}" != "pull_request" ]; then
              echo "- GitHub Pages not deployed"
            fi
            if [ -n "${TESTOPS_LAUNCH_URL:-}" ]; then
              echo "- [Allure TestOps launch](${TESTOPS_LAUNCH_URL})"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # Live GitHub Pages. Soft: a 503 must not fail generate. Retry: pages.yml.
  deploy-github-pages:
    name: deploy-github-pages
    needs: publish-allure-report
    if: >-
      always() && !cancelled() &&
      github.event_name != 'pull_request' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') &&
      needs.publish-allure-report.outputs.pages_artifact == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    concurrency:
      group: github-pages
      cancel-in-progress: false
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        continue-on-error: true
        uses: actions/deploy-pages@v5
