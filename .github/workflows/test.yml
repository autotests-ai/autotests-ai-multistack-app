# Leaf tests — called by ci.yml (layers=ci | prod-only) or workflow_dispatch / test_all.yml.
# Flip job-level `if: false` when wiring a deferred layer (see tests/LAYERS.md).
#
# layers: ci | prod-only | all-enabled
#   ci          → unit_backend + test-infra (+ component_rtl when enabled)
#   prod-only   → prod_api (+ prod_e2e when enabled)
#   all-enabled → ci + prod (manual / test_all)

name: reference-app-copy Tests

on:
  workflow_dispatch:
    inputs:
      layers:
        description: "ci | prod-only | all-enabled"
        required: false
        default: all-enabled
      include_python:
        description: "Run python backend unit matrix"
        type: boolean
        required: false
        default: false
  workflow_call:
    inputs:
      layers:
        type: string
        required: false
        default: all-enabled
      include_python:
        type: boolean
        required: false
        default: false

run-name: reference-app-copy tests (${{ inputs.layers || 'all-enabled' }} @ ${{ github.sha }})

concurrency:
  group: test-reference-app-copy-${{ github.ref }}-${{ inputs.layers || 'all-enabled' }}
  cancel-in-progress: true

jobs:
  unit_backend:
    name: unit_backend
    if: >-
      inputs.layers == 'ci' ||
      inputs.layers == 'all-enabled' ||
      inputs.layers == ''
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./backend/java/backend-java-spring/.github/actions/unit

  unit_backend_python:
    name: unit_backend_python (${{ matrix.stack }})
    if: >-
      inputs.include_python &&
      (inputs.layers == 'ci' ||
       inputs.layers == 'all-enabled' ||
       inputs.layers == '')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - stack: flask
            path: backend/python/backend-python-flask
          - stack: fastapi
            path: backend/python/backend-python-fastapi
          - stack: django
            path: backend/python/backend-python-django
    steps:
      - uses: actions/checkout@v7
      - uses: ./${{ matrix.path }}/.github/actions/unit

  test-infra:
    name: test-infra
    if: >-
      inputs.layers == 'ci' ||
      inputs.layers == 'all-enabled' ||
      inputs.layers == ''
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/test-infra

  component_rtl:
    name: component_rtl
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: ./frontend/typescript/frontend-typescript-react/.github/actions/component

  component_vue:
    name: component_vue
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: ./frontend/typescript/frontend-typescript-vue/.github/actions/component

  compose_up:
    name: compose_up
    if: false
    needs: [unit_backend, test-infra]
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - name: Start postgres + backend
        run: |
          docker compose up -d --build --wait
          curl -fsS "http://127.0.0.1:8800/api/health" | grep -q '"status":"ok"'

  api:
    name: api
    if: false
    needs: [compose_up]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/api

  integration:
    name: integration
    if: false
    needs: [compose_up]
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/integration

  e2e:
    name: e2e
    if: false
    needs: [api]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/e2e

  visual:
    name: visual
    if: false
    needs: [compose_up]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/visual

  manual:
    name: manual
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/manual

  prod_api:
    name: prod_api
    if: >-
      inputs.layers == 'prod-only' ||
      inputs.layers == 'all-enabled' ||
      inputs.layers == ''
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/prod-api

  prod_e2e:
    name: prod_e2e
    if: false
    needs: [prod_api]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/java/tests-java-gradle-junit5-allure3-selenide/.github/actions/prod-e2e

  tests_javascript_playwright:
    name: tests_javascript_playwright
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/javascript/tests-javascript-playwright/.github/actions/e2e

  tests_python_selenium:
    name: tests_python_selenium
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./tests/python/tests-python-selenium/.github/actions/e2e
