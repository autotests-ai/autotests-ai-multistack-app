# Generated from deploy/matrix.yaml — do not edit by hand; run render-vhosts.py
# Placeholders: backend_python_flask backend-python-flask.reference-app-copy.autotests.ai 8811 8801

upstream backend_python_flask_api {
    server 127.0.0.1:8811;
    keepalive 8;
}

upstream backend_python_flask_web {
    server 127.0.0.1:8801;
    keepalive 8;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name backend-python-flask.reference-app-copy.autotests.ai;

    # ssl_certificate ...;
    # ssl_certificate_key ...;

    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Content-Type-Options nosniff always;

    access_log /var/log/nginx/backend-python-flask.reference-app-copy.autotests.ai.access.log;
    error_log  /var/log/nginx/backend-python-flask.reference-app-copy.autotests.ai.error.log;

    location /api/ {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://backend_python_flask_api/api/;
    }

    location /frontend- {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://backend_python_flask_web;
    }

    location = / {
        return 404;
    }
    location / {
        return 404;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name backend-python-flask.reference-app-copy.autotests.ai;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
