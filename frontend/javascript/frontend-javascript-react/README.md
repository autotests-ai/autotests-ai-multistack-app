# frontend-javascript-react

Product UI — same reference-app screens as vanilla / TS React, implemented in **JavaScript + React**.

Slot: SPA source + build wiring TBD. Component tests → [`src/test/`](src/test/) (mirror of TS React).

**Stack page (when activated):** same pattern as `frontend-typescript-react` — route `/stack/` + `StackPage` using DS header/panels; matrix from `_shared/.../stack/matrix.json` (SSOT `deploy/matrix.yaml`).

Prod URL (planned): `https://reference-app-copy.autotests.ai/{backend}/frontend-javascript-react`
