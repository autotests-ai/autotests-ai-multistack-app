# frontend-javascript-jquery

Product UI — same reference-app screens as vanilla, implemented in **JavaScript + jQuery**.

Slot: static pages + jQuery wiring TBD. Future component tests → `src/test/`.

**Stack page (when activated):** same pattern as `frontend-javascript-vanilla` — shared `stack/index.html` shell + product header; matrix from `_shared/.../stack/matrix.json` (SSOT `deploy/matrix.yaml`).

Prod URL (planned): `https://reference-app-copy.autotests.ai/{backend}/frontend-javascript-jquery`
