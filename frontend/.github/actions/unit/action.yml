name: unit
description: Dispatch unit to the active module (module_dir).
inputs:
  module_dir:
    required: true
runs:
  using: composite
  steps:
    - uses: ./frontend/typescript/frontend-typescript-react/.github/actions/unit
      with:
        module_dir: ${{ inputs.module_dir }}
