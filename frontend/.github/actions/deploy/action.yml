name: deploy
description: Dispatch deploy to the active module (module_dir).
inputs:
  module_dir:
    required: true
  stand:
    required: true
runs:
  using: composite
  steps:
    - uses: ./frontend/typescript/frontend-typescript-react/.github/actions/deploy
      with:
        module_dir: ${{ inputs.module_dir }}
        stand: ${{ inputs.stand }}
