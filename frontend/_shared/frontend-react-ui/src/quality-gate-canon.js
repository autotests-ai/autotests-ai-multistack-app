/** Embed SSOT: `design-system/js/quality-gate.js` + `qg-info.js` (not a React rewrite). */
export {
  renderQualityGate,
  resolveQualityGateLabel,
  formatQualityGateRuleFormula,
  buildQualityGateInfoPayload,
  resolveQualityGateFileSource,
} from '../../design-system/js/quality-gate.js';

export { createQgInfo } from '../../design-system/js/qg-info.js';
