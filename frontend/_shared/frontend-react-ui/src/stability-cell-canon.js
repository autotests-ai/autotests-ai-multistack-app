/** Embed SSOT: `design-system/js/stability-cell.js` (dots + flaky badge, not a React rewrite). */
export {
  buildStabilityCell,
  stabilityStatusLabel,
} from '../../design-system/js/stability-cell.js';

export {
  sparklineThemeFromSite,
  statusSparkColor,
} from '../../design-system/js/sparkline.js';
