# frontend-typescript-jquery

Product UI — same reference-app screens as vanilla / JS jQuery, implemented in **TypeScript + jQuery**.

Slot: static pages + jQuery/TS wiring TBD. Future component tests → `src/test/`.

**Stack page (when activated):** same pattern as `frontend-javascript-jquery` — shared `stack/index.html` shell + product header; matrix from `_shared/.../stack/matrix.json` (SSOT `deploy/matrix.yaml`).

Prod URL (planned): `https://reference-app-copy.autotests.ai/{backend}/frontend-typescript-jquery`
