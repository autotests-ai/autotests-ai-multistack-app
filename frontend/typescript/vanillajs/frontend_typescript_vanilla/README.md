# frontend_typescript_vanilla

Product UI — same reference-app screens in **TypeScript vanillajs** (no React).

Slot: source TBD. Future component tests live beside this folder under `vanillajs/`.

Prod URL (planned): `https://{backend}.reference-app-copy.autotests.ai/frontend_typescript_vanilla`
