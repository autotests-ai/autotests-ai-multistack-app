# Component tests (slot)

Vue unit / component tests for this product module (Vitest + Vue Test Utils / Testing Library).
Mirror of `frontend-typescript-react/src/test/` when the Vue SPA is filled in.
