# tests_typescript_react-testing-library

Vitest + React Testing Library for [`../frontend_typescript_react/`](../frontend_typescript_react/).

| | |
|--|--|
| Pyramid job | `component_rtl` ([`tests/LAYERS.md`](../../../../tests/LAYERS.md)) |
| Allure | `layer=component`, `scope=react`, `framework=react-testing-library` |

```bash
# when package.json is present:
npm test
```

Not the browser `@Tag(component)` slice (that hits `frontend/_catalog/frontend_javascript_preview` via Selenide).
