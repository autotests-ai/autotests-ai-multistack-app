# Backend — Go

Two teaching stacks, same OpenAPI contract as `backend-java-spring`:

| Module | Status | Notes |
|--------|--------|-------|
| [`backend-go-gin`](backend-go-gin/) | slot | [Gin](https://github.com/gin-gonic/gin) — usual product REST course stack |
| [`backend-go-stdlib`](backend-go-stdlib/) | slot | `net/http` + `ServeMux` — same style as Selenoid hub (no framework) |

Routing SSOT: [`deploy/matrix.yaml`](../../deploy/matrix.yaml).
