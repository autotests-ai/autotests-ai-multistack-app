# static/ (generated)

Do not edit or commit files under `static/`.

SSOT:
- `frontend/_shared/frontend_javascript_app/` — lean DS runtime
- `frontend/javascript/vanilla/frontend_javascript_vanilla/` — product pages

Materialize:

```bash
./backend/scripts/sync-app-static.sh
```

Docker/`bootJar` image builds run the same sync before packaging.
