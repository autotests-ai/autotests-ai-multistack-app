# Skip the configuration phase when nothing it depends on changed. This pays off locally, on
# repeat runs; CI gets no benefit, because a runner computes a different entry id on every run
# and can only store a fresh entry. Caching the directory does not help — verified.
org.gradle.configuration-cache=true
