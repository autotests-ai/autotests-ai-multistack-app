name: Unit tests backend-python-flask
description: pip install + pytest for backend-python-flask

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v6
      with:
        python-version: "3.12"
        cache: pip
        cache-dependency-path: backend/python/backend-python-flask/requirements.txt

    - name: Install + pytest
      shell: bash
      working-directory: backend/python/backend-python-flask
      run: |
        python -m pip install -r requirements.txt
        python -m pytest
