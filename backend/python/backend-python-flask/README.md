# backend-python-flask

Python Flask JSON API — same OpenAPI contract as `backend-java-spring`
(`/api/health`, items, auth/JWT). Postgres DB: `reference_app_python_flask`.

**Status:** stub — minimal `/api/health` for Host-based routing until Flask is implemented.

```
https://backend-python-flask.reference-app-copy.autotests.ai/{frontend}/
```

Shared UI (one `web` image): `/frontend-typescript-react/`, `/frontend-javascript-vanilla/`, …
