# Backend — TypeScript

Runtime is Node; **language** in the matrix is `typescript` (same axis as frontend).

| Module | Status | Notes |
|--------|--------|-------|
| [`backend-typescript-nest`](backend-typescript-nest/) | slot | [NestJS](https://nestjs.com/) — TypeScript product/enterprise stack |

JavaScript twin: [`../javascript/`](../javascript/).  
Routing SSOT: [`deploy/matrix.yaml`](../../deploy/matrix.yaml).
