# backend-node-express

Node [Express](https://expressjs.com/) JSON API — same OpenAPI contract as `backend-java-spring`.  
Postgres DB: `reference_app_node_express`.

**Status:** slot (not wired in compose until implemented).

```
https://reference-app-copy.autotests.ai/backend-node-express/{frontend}/
```

Why Express (not Koa/Fastify first): most common course/tutorial stack for REST + middleware.  
Sibling with structure: [`backend-node-nest`](../backend-node-nest/) (NestJS).
