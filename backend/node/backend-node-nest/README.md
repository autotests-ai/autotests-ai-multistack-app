# backend-node-nest

Node [NestJS](https://nestjs.com/) JSON API — same OpenAPI contract as `backend-java-spring`.  
Postgres DB: `reference_app_node_nest`.

**Status:** slot (not wired in compose until implemented).

```
https://reference-app-copy.autotests.ai/backend-node-nest/{frontend}/
```

Pair with [`backend-node-express`](../backend-node-express/): Express = thin intro REST; Nest = TypeScript modules/DI product stack.
