name: integration
description: Dispatch integration to the active module (module_dir).
inputs:
  module_dir:
    required: true
runs:
  using: composite
  steps:
    - uses: ./backend/java/backend-java-spring/.github/actions/integration
      with:
        module_dir: ${{ inputs.module_dir }}
