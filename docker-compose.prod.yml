# Prod override — merge: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Canon: docs/docker/DOCKER-CANON.md · reboot: docs/docker/prod-reboot.md

services:
  postgres:
    restart: unless-stopped
  backend-java-spring:
    restart: unless-stopped
  frontend-typescript-react:
    restart: unless-stopped
  stand-gateway-ci:
    restart: unless-stopped
